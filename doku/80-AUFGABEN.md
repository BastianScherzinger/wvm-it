---
bereich: aufgaben
titel: Aufgaben
stand: 2026-09-12
status: teilweise
fortschritt: 82
zusammenfassung: Stand 12.09.2026 (Einstieg docs/STAND-2026-09-10.md). Zuletzt Paket 193, auf demselben Zweig sofort/2026-09-12-bf18-und-2-weitere: BF18 anders eingebaut als gemeldet -- die Messung nennt unveraendert .err-code, das seit Paket 184 geheilt ist. Gebaut wurden die drei Regeln, die Paket 189 ausdruecklich hat liegen lassen, weil sie ihre Goldfarbe erst auf Eingabe setzen und deshalb in keiner Lighthouse-Einzelpruefung stehen: .rg-sw-link:hover strong und :focus-visible strong auf allen sieben Regionsseiten, .fld-recht a:hover in jedem Anfrageformular, .ub-fakten a:hover auf /ueber-uns/. Gemessen sind das dieselben 2,02:1 auf Weiss wie bei den dauerhaften Faellen, und :focus-visible ist der Zustand, in dem eine Tastaturbedienung dauerhaft steht; alle drei tragen jetzt --accent-ink, die Ausklammerung der Zustaende im Test ist entfallen, Suite 297 auf 298. SI03 ist nicht moeglich: includeSubDomains steht seit dem 05.09.2026 und geht laut Messbeleg live mit, preload fehlt allein daran, dass die Apex-Domain wvm-it.tech beim Registrar parkt (A-Record 213.145.224.30, Beim Kunden Nr. 3) -- ein Eintrag in die Preload-Liste machte sie dauerhaft unerreichbar, und aus dieser Liste kommt man nur schwer wieder heraus. SU05 als begruendete Ausnahme eingetragen, ohne eine Zeile Code: Weitere Ortsseiten sind in SEO-AUSBAU-3.md ausdruecklich verworfen (Doorway-Pages, Regel A16), und den echten Auftrag aus einem achten Ort, den der Rat selbst verlangt, gibt es nicht. Davor Paket 189: BF18 anders eingebaut als gemeldet -- das Element der Messung war bereits geheilt (.err-code aus Paket 184, das inzwischen auf main liegt; gemessen wurde die Live-Adresse). Geprueft wurde stattdessen die Regel, die der Kopf von style.css seit dem Umbau 2026-08 aufstellt: Gold als Text auf Hell nur ueber --accent-ink. Geschrieben stand sie, geprueft wurde sie nie, und zwei Regeln hielten sich nicht daran -- .marquee-track i mit Deckkraft darueber (laut Commit 1,69:1) und .rg-km auf Weiss (2,02:1), beide dauerhaft sichtbar. Beide tragen jetzt --accent-ink, GoldAlsTextTest verhindert den Rueckfall und liest dabei die Eigenschaft statt der Zeichenkette; Suite 297 in 19 Dateien. IS19 eingebaut: Die eine duenne Seite von 198 war /impressum/, laut Commit von 152 auf 337 Woerter gewachsen -- vor allem um die Blattlinie nach § 25 Abs. 4 MedienG, auf die sich die erste Zeile der Seite beruft und die dort nie stand. Geaendert ist ein Feld in content.json; die neuen Angaben stehen nach einer Nachbesserung in den vorhandenen neun Absaetzen, weil vier eigene Absaetze ueber |linebreaks vier zusaetzliche p-Elemente erzeugten und die Designwache das als Aufbauaenderung meldete; UID und Kammer fehlen weiter und sind bewusst nicht geraten. PF13 als begruendete Ausnahme eingetragen, ohne eine Zeile Code: Das Jahr steht laengst, gemeldet wird allein das fehlende immutable, und das schliesst derselbe Rat ohne Hash im Namen selbst aus -- dabei ist in 70-PERFORMANCE.md eine falsche Angabe berichtigt worden (weder Hash im Namen noch Manifest-Storage). Davor Paket 184, inzwischen auf main: BF18 eingebaut -- das einzige Element, das die Messung noch mit zu wenig Kontrast fuehrte, war .err-code, die grosse Zahl auf der 404- und der 500-Seite. Falsch war nicht die Farbe, sondern opacity:.5 darueber: Deckkraft mischt mit dem Grund, aus #d8a43d auf #12100c wurde effektiv #755a24, und das sind 2,94:1 statt der 3:1, die grossem Text zustehen. Eine Deckkraft unter 1 ist eine Farbaenderung, und die sieht ein Token-Test nicht -- deshalb kam der Fall durch alle bisherigen Pruefungen; die Rechnung in dieser Datei ging zudem von hellem statt dunklem Grund aus. Ohne die Deckkraft haelt dieselbe Farbe 8,41:1, nachgerechnet von ErrCodeKontrastTest (Suite 292 auf 295). VL21 (Angebotsbaustein in einen Teil-Ordner) und VL13 (alle Formulare per POST) sind als begruendete Ausnahmen eingetragen, ohne eine Zeile Code: Der Baustein steht schon heute genau einmal und wird 13-mal eingebunden -- ein Umzug wuerde ueber stand_schreiben 140 der 198 Adressen ein Aenderungsdatum geben, an dem sich kein Inhalt geaendert hat; und die drei Formulare ohne POST sind Abfragen (Kurzrechner, Kostenrechner, Selbsttest), bei denen GET die richtige Methode ist und POST das verlinkbare Ergebnis kostete. Davor Paket 170, liegt auf dem Zweig sofort/2026-09-11-vl01-und-2-weitere: PJ11 anders eingebaut als vorgeschlagen -- das vorhandene requirements.lock gilt jetzt auch beim Deploy, weil requirements.txt es per --constraint einbindet (Nixpacks installiert nur aus requirements.txt); ob der Railway-Bau damit durchlaeuft, zeigt erst der erste Deploy nach dem Merge, und die Messregel sieht das Lockfile nicht, weil das Pruefwerkzeug keine .lock-Dateien einliest. VL01 (kein apps/, kein data/) und IS06 (Titel mit Ort oder Nutzen) sind als begruendete Ausnahmen eingetragen, ohne eine Zeile Code: Ein Umzug nach apps/ setzte alle Aenderungsdaten zurueck, und die Titelregel kennt als Ort nur Lenzing. Davor Paket 153: Vier Seiten sagen jetzt dreisprachig, was Fernwartung, Festpreis, Datensicherung und KI-Automatisierung bedeuten (GE26) -- je ein Satz im vorhandenen Absatz, kein neues Element, kein neuer Link; die Rechtstexte bewusst ohne, weil ein Definitionssatz dort eine Aussage ohne Deckung waere. PF17 und IS18 sind im selben Durchgang als begruendete Ausnahmen eingetragen, ohne eine Zeile Code. Davor in vier Tagen von 165 auf 198 URLs und von 130 auf 282 Testfunktionen in 18 Testdateien -- zuletzt am 10.09. sechs dazu, weil PJ03 nicht an den Tests scheiterte, sondern am gebuendelten Import-Block, den eine Quelltext-Analyse nicht als Modulbezug liest. Groesster Zuwachs ist das neue Silo /einrichten/ mit acht Seiten dreisprachig: Es existiert, weil alle dreizehn Leistungsseiten um laufende Betreuung oder ein Projekt gebaut sind und fuer 'ein einzelnes Geraet, jetzt, ohne Vertrag' keine Seite da war -- obwohl der Preis dafuer seit jeher im Katalog stand. Dazu drei Ratgeber, die jeweils auf GENAU EINE Einrichtungsseite mit Preis fuehren. Davor repariert: 397 HTML-Entities in den Sprachpaketen, der Titel der deutschen Startseite ohne Firmennamen, ein Einstieg fuer 490 statt 190 Euro, zwei Rechtstexte mit ueberholten Aussagen und die Datenverlust-Luecke an allen vier Formularwegen. Offen bleiben sechs Angaben, die nur Florin liefern kann -- darunter zwei Pflichtangaben nach § 5 ECG -- sowie die Frage, ob nach dem Fix vom 12.09.2026 ueberhaupt noch ein Kontrastelement gemeldet wird. Neu offen aus diesem Durchgang: Die Barrierefreiheitserklaerung raeumt in Abschnitt 3 weiter fuenfzehn gemeldete Elemente und eine laufende Ursachensuche ein -- beides ist seit dem Fix ueberholt, und ein Rechtstext ist eine Zusage; nachzuziehen ist der Satz aber erst nach dem naechsten Messlauf, damit die neue Zahl gemessen und nicht geschaetzt ist.
offen: 5
quellen: docs/BEFUNDE-281-2026-09-06.md, docs/AUSBAU-2026-09.md, docs/SEO-AUSBAU-3.md, docs/SEO-PLAN.md, docs/AUSBAU-2026-08.md, docs/SEO-KONZEPT-DACH.md, docs/DEPLOY.md
---

# Aufgaben

*Woran sich der Fortschritt bemisst: am Anteil der erledigten an allen in dieser Datei geführten Aufgaben — „Erledigt“ gegen „Erledigt + Offen + Fehlt + Beim Kunden“, auf Zehner gerundet. „Verbesserungsmöglichkeiten“ zählen nicht mit, sie sind Kür, keine Zusage. Bei allen sechs betreuten Seiten dieselbe Rechnung.*

> **Ausgangspunkt:** Aus den Plänen im Projekt ist im Code nichts mehr offen —
> `SEO-AUSBAU-3.md` steht auf 56 von 56, `AUSBAU-2026-09.md` hat am 05.09.2026 die
> Messung vom 04.09. abgearbeitet. Alles unten kommt aus der **Messung**, aus dem
> Betrieb (Search Console, Deploy) oder liegt beim Kunden. Regelkennungen in Klammern
> lassen sich im Werkzeug nachschlagen.

## Missbrauchsschutz am Formular (Stand 05.09.2026, „Erst speichern, dann mailen" am 07.09.2026 nachgezogen)

Aus dem Pflichtabschnitt in [10-TECHNIK.md](10-TECHNIK.md). Am 04.09. war keiner
dieser Bausteine im Quelltext zu finden; fünf davon gab es in anderer Form bereits,
drei sind am 05.09. dazugekommen.

- [x] **Zeitfalle** — der Honigtopf `website` (`templates/honigtopf.html`); ein Feld
      namens `hp` war als Falle erkennbar und wurde von Bots übersprungen
- [x] **Rate-Limit je IP** — `views._limit_erreicht`, je Bereich getrennt, seit
      28.08.2026; die letzte Adresse aus `X-Forwarded-For`, nicht die erste.
      **Die Erhebung vom 04.09. hat das übersehen**, weil sie nach dem Namen der
      Hauptseite suchte: Eine Suche nach Namen misst Namen, nicht Wirkung
- [x] **Datenschutzhinweis am Formular** — seit 05.09.2026 in allen zehn
      Anfrageformularen, von `pruefe_seite` erzwungen
- [x] **Prüfbefehl für die Abwehr** — `manage.py pruefe_sicherheit`, zehn Prüfungen,
      löst alle Formulare wirklich aus und zählt die Mails
- [x] **Feldlängen begrenzt** — `views._feld(..., grenze)`
- [x] **Betreff gesäubert** — `views._betreff()` entfernt Zeilenumbrüche
      (Header-Injection)
- [ ] **Inhalts-Score mit Schwelle** — heute wird nur auf E-Mail oder Telefonnummer
      geprüft, nicht auf den Inhalt
- [x] **Erst speichern, dann mailen** — seit 06.09.2026: `views._anfrage_sichern`
      legt jede Anfrage als Zeile JSON ab und druckt sie ins Log, **bevor** die Mail
      rausgeht. Scheitert der Versand, ist die Anfrage nicht mehr weg.
      **Seit 07.09.2026 (`MW18`) an allen Anfragewegen:** Kontaktformular
      (`_handle_contact`), Angebots-Konfigurator (`_handle_angebot`), Richtangebot der
      Startseite (`angebot_anfordern`) und Kooperationsanfrage (`kooperation_anfordern`)
      hingen bis dahin nicht daran — also ausgerechnet die vier Wege, über die die
      ausführlichen Anfragen kommen. `anfrage_absenden` bleibt bewusst aussen vor: Dieser
      Weg legt den Vorgang in `supa.enqueue_job` an und ist damit schon aufbewahrt
- [x] **Die Falle protokolliert ihre Treffer** — seit 06.09.2026. Vorher war ein
      Fehlalarm nach beiden Seiten unsichtbar: Der Absender sah „Angekommen", das
      Postfach blieb leer, gezählt wurde nichts. Zusätzlich entscheidet jetzt der
      **Inhalt** des Feldes: die eigene Adresse kommt aus der Ausfüllhilfe des
      Browsers und geht durch, eine fremde bleibt ein Bot
- [ ] **Mail-Obergrenze je Tag** — die Bremse zählt je Bereich und Fenster, nicht
      je Tag über alle Bereiche

## Offen

Konkret als Nächstes, in dieser Reihenfolge.

| # | Aufgabe | Warum jetzt | Regel / Quelle |
|---|---|---|---|
| 1 | **Core Web Vitals eintragen** in `../docs/seo/PERFORMANCE.md` §3 und den **CLS-Ausreißer auf Desktop** untersuchen: `/leistungen/` 0,180 · `/kosten/rechner/` 0,184 · `/kontakt/` 0,229 bei mobil nahezu null | Der einzige Core-Web-Vitals-Wert, der wirklich reißt. Die Tabelle ist seit dem 29.08. leer | T8, `PF08` |
| 2 | **Mittlere Antwortzeit senken** (Median 632 ms, Startseite 3.123 ms im Crawl): Dienst warmhalten, Seitencache für die Ansichten ohne Formular | Betrifft Crawlbudget und echte Besucher gleichermaßen. Symbolsatz und kleinere Bilder haben die Größe gesenkt, nicht die Wartezeit auf die erste Antwort | `PF10`, `BT04` |
| ~~3~~ | ~~**Die 32 Kontrastelemente einzeln nachmessen**~~ — **beantwortet am 06.09.2026: die Eigenmessung vom 27.08. war die falsche.** `--ink-dim` stand hell auf `#8a8177` und erreichte auf `--bg-2` nur **3,40:1**. Das Token wird 57-mal verwendet, im Dunkelmodus hielt es 6,04:1 — deshalb fiel es beim Draufschauen nie auf. Neu `#746c64`, 4,58:1 auf dem dunkelsten Grund | erledigt, `BF18` |
| ~~4~~ | ~~**Barrierefreiheitserklärung widerspricht sich selbst**~~ — **erledigt am 07.09.2026, Commit `7b7207a`.** Abschnitt 2 sagt jetzt, was gemessen und automatisch nachgerechnet wird (die **Farbwerte**, in beiden Erscheinungsbildern); Abschnitt 3 nennt Zahlen statt eines Vorbehalts: 32 Elemente am 06.09., eine Textfarbe bei 3,4 statt 4,5, korrigiert, seither 15 — und dass diese 15 nicht mehr aus den Farbwerten stammen | erledigt, Loop A |
| ~~5~~ | ~~**Die 15 verbliebenen Kontrastelemente im Browser messen.**~~ — **der eine, der übrig war, ist am 12.09.2026 behoben.** Die Messung dieses Tages nennt statt fünfzehn nur noch **ein** beanstandetes Element, und es ist der Kandidat, der hier seit dem 07.09. stand: `.err-code`, die grosse Zahl auf der 404- und der 500-Seite. Die Rechnung an dieser Stelle war allerdings falsch — sie ging von hellem Grund aus (1,4:1); beide Vorlagen setzen die Zahl in einen Kopf mit `class="sp-top on-dark"`, und dort ergibt `--accent` bei `opacity:.5` effektiv #755a24 auf #12100c, also **2,94:1**. Damit riss sie selbst die 3:1 für grossen Text. Die Deckkraft ist entfernt, dieselbe Farbe hält jetzt 8,41:1; `ErrCodeKontrastTest` in `landing/tests/test_kontrast.py` rechnet die Mischung nach, statt nur das Token zu lesen. Der Merksatz dazu: **Eine Deckkraft unter 1 ist eine Farbänderung, und die sieht ein Token-Test nicht.** Ob damit null bleiben, zeigt der nächste Messlauf | erledigt, `BF18` |
| 6 | **Impressum: Kammer und Berufsbezeichnung fehlen.** § 5 Abs. 1 ECG verlangt Z 6 die Kammer, der der Diensteanbieter angehört, und Z 7 die Berufsbezeichnung samt verleihendem Staat. Das Impressum nennt Gewerbebehörde (BH Vöcklabruck) und Gewerbeordnung mit RIS-Link — die **Wirtschaftskammer** (Pflichtmitgliedschaft jedes österreichischen Gewerbetreibenden, hier WKO Oberösterreich) und die Berufsbezeichnung fehlen. Braucht eine Angabe von Florin: Fachgruppe und, falls vorhanden, GISA-Zahl. **Am 12.09.2026 nachgeprüft und unverändert offen:** Das Impressum ist an diesem Tag von 152 auf 337 Wörter gewachsen (`IS19`), aber **um keine dieser beiden Angaben** — sie wurden ausdrücklich nicht geraten, weil eine erfundene Kammer oder Berufsbezeichnung schlechter wäre als die Lücke. Neu darin sind Blattlinie, Verantwortlicher, Tätigkeitsgebiet, Linkhaftung und Urheberrecht | Loop B 07.09., abgeleitet aus `EIG02` |
| ~~7~~ | ~~**Impressum verweist auf die abgeschaltete OS-Plattform**~~ — **erledigt am 07.09.2026, Commit `7b7207a`.** Der Hinweis nennt jetzt die Einstellung zum 20.07.2025; der Teil, der Pflicht bleibt (Schlichtungsbereitschaft), steht weiter da | erledigt, `EIG03` |
| ~~8~~ | ~~**Zwei Video-Poster im alten Format**~~ — **erledigt am 07.09.2026, Commit `7b7207a`.** `pc_poster` −41 %, `server_poster` −46 %. Der neue Test prüft `content.json` selbst und führt die zwei erlaubten Ausnahmen mit Begründung | erledigt, `PF15` |
| ~~9~~ | ~~**Der Einstieg auf der EDV-Hauptseite ist der teuerste statt der naheliegendste**~~ — **erledigt am 07.09.2026, Commit `7b7207a`.** Von 490 € (Sicherheitscheck) auf 190 € (Neuen Arbeitsplatz einrichten). Ein Zeilenwechsel, kein neuer Text — `_einstieg_daten()` zieht Name, Text und Preis aus dem Katalog. Auf `/leistungen/it-sicherheit/` bleibt der Check der richtige Einstieg | erledigt, `KV05` |
| ~~10~~ | ~~**Der Gerätelebenszyklus fehlt als Thema — sechs Suchanfragen ohne jede Seite.**~~ — **erledigt am 08.09.2026, Commits `f355374`, `6c3627f`, `8bb5b67`, `f578c73`.** Das Silo `/einrichten/` beantwortet „wer macht mir das jetzt?" und führt acht Seiten plus Hub (`landing/einrichtungen.py`), darunter `arbeitsplatz` und `pc-tausch` — die beiden Seiten zur 190-€-Position; dazu die Vergleichsseite `/vergleich/pc-aufruesten-oder-neu-kaufen/` (`landing/vergleiche.py:56`). „aufrüst" und „Datenübernahme" kamen im ganzen Projekt null Mal vor und stehen jetzt in `landing/i18n/einrichten_de.py` und `vergleiche_de.py` | erledigt, `SU08`/`IS18` |
| ~~11~~ | ~~**Windows-10-Ende ist der dringlichste Einzelfall daraus.**~~ — **erledigt am 08.09.2026, Commit `f578c73`.** Den Beitrag gibt es unter `/aktuelles/windows-10-ende-was-jetzt/` (`landing/beitraege.py:124`); er hängt über `"einrichtung": "pc-tausch"` an der Seite, auf der der Preis steht, und wurde am selben Tag einzeln zur Indexierung beantragt (Commit `7f784a8`) | erledigt |
| 12 | **Was bei einem Hardwaredefekt passiert, steht nirgends.** Die Seite ist konsequent auf Tausch ausgelegt, nicht auf Reparatur — das ist vermutlich Absicht, aber sie sagt es nicht. „Hardware tauschen" steht als **einzelner Stichpunkt** in `landing/i18n/regionen_de.py:36`. Wer „Hardware Reparatur Vöcklabruck" sucht, findet nichts, das ihm sagt, ob er hier richtig ist. Eine Frage im FAQ (Diagnose, Austausch, keine Werkstattreparatur) verhindert Enttäuschung statt Leere | Loop B 07.09. |
| 13 | **Die Barrierefreiheitserklärung ist dem Fix vom 12.09.2026 nicht nachgezogen.** Abschnitt 3 sagt weiter, die Zahl der gemeldeten Elemente sei „auf fünfzehn gesunken", diese fünfzehn stammten „aus einzelnen Kombinationen im fertig dargestellten Bild" und würden „derzeit nachgemessen und behoben, sobald die Ursache je Element feststeht" (`content.json`, Abschnitt „Nicht barrierefreie Inhalte" — am 12.09.2026 im Quelltext nachgelesen). Beides ist überholt: Die Messung dieses Tages führte **ein** Element, die Ursache steht fest (Deckkraft statt Farbwert), und behoben ist es. Ein Rechtstext ist eine Zusage — er darf keinen Rückstand einräumen, den es nicht mehr gibt, und ebenso wenig eine Ursachensuche, die abgeschlossen ist. Nachzuziehen in einem Lauf, der `content.json` anfassen darf, und **nach** dem nächsten Messlauf: Die neue Zahl gehört gemessen, nicht geschätzt | `BF18`; `../CLAUDE.md`, Zeile „Rechtstexte" |

## Fehlt

Noch nicht begonnen — kein Plan, kein Anfang.

| Was | Wirkung | Regel |
|---|---|---|
| **Fehler-Monitoring** (Sentry o. ä., DSN aus der Umgebung) | Der CI-Lauf steht seit 05.09.; was im Betrieb schiefgeht, sieht weiterhin nur, wer ins Railway-Log schaut | `VL19` |
| **Zweite und dritte Fallstudie** (Rhein-Neckar, RTC-Service, FSH GmbH) — braucht das Einverständnis der Kunden | Bisher nur Rümpelwerk als Referenz; `/referenzen/` ist der einzige Themenbereich mit einer einzigen Seite | `SEO-PLAN.md` T3, `SU08` |
| **Verzeichniseinträge** (WKO Firmen A–Z, Herold.at, Bing Places, Apple Business Connect) mit zeichengleicher NAP | Voraussetzung für `sameAs` — das steht heute leer, und das ist richtig so, solange es keine echten Profile gibt. Bing speist die Websuche von ChatGPT. **Am 05.09.2026 erneut angesehen und als am Rechner nicht lösbar bestätigt:** Der Schema-Knoten ist vorbereitet (S7) und rendert, sobald `content.json` → `profile` gefüllt ist; eine geratene Adresse in `sameAs` wäre schlechter als ein leeres Feld | `SEO-PLAN.md` T6, `GE11` |
| **Zwei Fachbeiträge im Monat** als eingehaltener Takt | T2 ist begonnen, der Takt hat sich noch nicht bewährt. Seit 05.09. gibt es dafür einen Feed unter `/feed/` | `SEO-PLAN.md` T2 |
| **Seitencache** für die Ansichten ohne Formular | Der größte verbliebene Hebel bei der Antwortzeit; heute wird jede Seite bei jedem Aufruf neu gebaut, obwohl sich nichts ändert | `PF10` |

## Verbesserungsmöglichkeiten

Aus den offenen Regeln der Messung vom 02.09.2026, nach Hebel sortiert. Der Prozentwert in Klammern ist der Erfüllungsgrad der Regel heute.

| Hebel | Regel | Befund | Was zu tun ist |
|---|---|---|---|
| **1. Antwort zuerst** | `GE23` (kritisch, 9 %) | nur **14 von 158 Seiten** beginnen mit einer zitierfähigen Antwort — schwach unter anderem `/`, `/kontakt/`, `/en/kontakt/`, `/ro/kontakt/`, `/angebot/` („weder Definition noch Zahl") | Jede Seite mit zwei bis drei Sätzen eröffnen, die die Frage der Überschrift beantworten und **eine Zahl oder eine Festlegung** enthalten. Antwortmaschinen zitieren fast immer den ersten sachlichen Absatz — der grösste GEO-Hebel der Seite. **Am 05.09.2026 nachgezogen: 14 Glossareinträge und 4 Fachbeiträge** tragen jetzt eine Zahl, die aus dem Eintrag selbst oder aus `ANGEBOT_GROUPS` stammt. **Am 08.09.2026 für die vier Rechtsseiten gebaut, aber nicht ausgeliefert** (`GE23`, Commit `87f7dc3`). ⚠ **Liegt auf `sofort/2026-09-08-ge23-und-2-weitere`, nicht auf `main`** (nachgemessen am 10.09.2026: `git branch --contains 87f7dc3` nennt nur diesen Zweig, `templates/recht.html` bindet auf `main` kein `antwort.html` ein). Das Paket wurde abgewiesen, weil der Absatz auch auf `/impressum/` und `/datenschutz/` erschien; die Entscheidung darüber steht aus. Inhaltlich: Impressum, Datenschutz-, Barrierefreiheitserklärung und AGB begannen mit ihrer Formzeile — „Offenlegung gemäß § 5 ECG und § 25 MedienG.", neun Wörter, die die Frage der Überschrift nicht beantworten. Auf dem Zweig bindet damit jede Seitenvorlage ausser `index.html` `templates/antwort.html` ein; **auf `main` gilt das für `templates/recht.html` nicht.** Offen bleibt die Startseite |
| **2. Tests** | `PJ02` (kritisch, 0 %), `PJ03` (26 %), `PJ04` (0 %), `VL19` (43 %) | keine einzige Testfunktion; 34 von 46 Modulen berührt kein Test; 3 von 7 QS-Bausteinen erfüllt **Erledigt seit dem 05.09.2026: 130 Testfunktionen in neun Dateien**, CI-Lauf bei jedem Push — siehe die Ausnahme zu `PJ02` unten und [10-TECHNIK.md](10-TECHNIK.md). Von den sieben QS-Bausteinen (`VL19`) fehlt nur noch das Fehler-Monitoring. **Am 07.09.2026 auch `PJ03` geschlossen:** Die Suite prüfte Seiten und kam über `views` an den Strukturquellen nur vorbei; `landing/tests/test_module.py` fasst sie jetzt direkt an — die Strukturquellen der Silos, die achtzehn Sprachmodule, `messung`, `supa`, `scheduler`, `apps` und die eigenen Management-Befehle. Damit **252 Testfunktionen in 17 Dateien**. **Am 10.09.2026 nachgebessert:** Die Messung meldete dieselben 34 Module weiter als von keinem Test berührt — nicht weil die Prüfung fehlte, sondern weil der Import-Block sie gebündelt aufführte und eine Quelltext-Analyse davon nur das Paket vor dem `import` sieht. Jetzt ein Modul je Zeile, dazu `landing/middleware.py` samt seinen vier reinen Funktionen und drei Funktionen aus `landing/supa.py` im Fall ohne Zugang: **282 Testfunktionen in 18 Testdateien** |
| **3. Interne Erreichbarkeit** | `TS23` (kritisch, 48 %) | 82 Seiten über interne Links nicht erreichbar: `/en/kontakt/`, `/ro/kontakt/`, `/en/angebot/`, `/ro/angebot/`, `/en/impressum/` … (+77) | siehe „Offen" Nr. 2 |
| **4. Umfang der Seiten** | `IS18` (kritisch, 34 %), `IS19` (kritisch, 40 %), `IS17` (82 %) | 55 von 84 Seiten unter dem Zielumfang ihrer Art (`/leistungen/` 328/600 W, `/vergleich/` 288/900 W, `/branchen/` 300, `/leistungen/google-ads/` 583/600); 11 dünne Seiten (Kontakt 140–149 W, Impressum 137 W je Sprache); 28 Seiten unter 300 Wörtern | Hubs mit Auswahlhilfe, Ablauf und Fragen füllen; Kontaktseiten um Anfahrt, Erreichbarkeit und Ablauf ergänzen; das Impressum darf kurz bleiben. **`IS18` ist am 10.09.2026 als Ausnahme eingetragen** (siehe „Bewertung der Messpunkte"): Vier der fünf namentlich genannten Seiten liegen bei 83 bis 98 Prozent ihres Solls, die fünfte ist mit `/branchen/` ein Wegweiser — Füllung ist auf dieser Seite seit E7 als Fehler festgelegt. Wo Umfang wirklich fehlte, ist er im selben Paket gewachsen: `GE26` hat vier Seiten dreisprachig um einen Definitionsabsatz ergänzt. **`IS19` ist am 12.09.2026 abgearbeitet:** Die Messung dieses Tages führte noch **eine** dünne Seite von 198, `/impressum/`, und die ist laut Commit von 152 auf 337 Wörter gewachsen — ausschliesslich um Angaben, die nach § 5 ECG und § 25 MedienG ohnehin dorthin gehören, allen voran die **Blattlinie**, auf die sich die erste Zeile der Seite beruft und die dort nie stand. Offen bleibt davon unberührt `IS17` |
| **5. Beinahe-Duplikate** | `IS21` (kritisch, 0 %) | 6 Seitenpaare über 60 % Textgleichheit, Höchstwert 100 %: Impressum und Datenschutz sind in DE, EN und RO wortgleich (die Rechtstexte bleiben bewusst Deutsch, AT-Rechtslage) | Entscheiden statt liegen lassen: entweder die EN/RO-Rechtstexte übersetzen oder sie auf `noindex` mit Canonical auf die deutsche Fassung setzen |
| **6. Kritische Datei-Befunde** | `PJ05` (kritisch, 0 %), `PJ06` (60 %), `PJ07` (38 %), `PJ08` (25 %) | 448 Befunde auf 24.653 Zeilen (18,2 je 1.000), davon 9 kritisch und 375 wichtig; dichteste Dateien `landing/views.py` (37), `newsletter_confirm.html` (12), `anfrage_done.html` (11); häufigste Klassen: Ausgabe ohne Maskierung 326×, Modul ohne Test 34×, `print()` 23× **Die kritischen sind seit dem 05.09.2026 abgearbeitet** — zuletzt die fünf verbliebenen verschluckten Ausnahmen (`PJ05`, Commit `fe88da4`). Bleiben die Hinweisklassen: Die `\|safe`-Ausgaben sind laut `docs/mehrsprachigkeit.md` Absicht (vertrauenswürdige Sprachpakete) — diese Entscheidung gehört als Freibrief dokumentiert. **Am 07.09.2026 nachgezählt und als `PJ08` begründet abgelehnt** (siehe „Bewertung der Messpunkte"): Der einzige Anteil, der sich ohne Rücknahme einer bewussten Entscheidung abräumen liess, waren die Module ohne Test — und die sind seit demselben Tag getestet (`PJ03`), seit dem 10.09.2026 auch so, dass die Messung es sieht. **`PJ07` ist am 10.09.2026 nachgezählt und ebenfalls als Ausnahme eingetragen:** Der Anteil steigt erst, wenn eine Datei **alle** ihre Befunde verliert, und 42 der 46 Vorlagen enthalten `\|safe` |
| **7. Schema-Graph** | `VL10` (64 %), `GE11` (0 %), `GE22` (0 %), `GE14` (60 %), `GE12` (99 %), `GE13` (92 %), `GE07` (82 %) | kein einziger `sameAs`-Verweis („die Seite steht als Insel da"), keine Geokoordinaten, `WebSite` ohne `SearchAction` obwohl `/suche/` existiert, 24+ offene `@id`-Verweise aus `/wissen/`, `/en/` und `/ro/` ohne Brotkrumen, Leistungs-Hubs ohne `Service` | Betriebsknoten um `geo`, `openingHoursSpecification` und `sameAs` ergänzen; jeden `@id`-Verweis auf einen Knoten im Graphen richten |
| **8. Autorschaft und Article** | `GE15` (32 %), `GE16` (32 %) | 15 von 47 Ratgeberseiten als `Article`, 15 von 47 mit Autor — Vergleiche, Checklisten und Glossar haben keinen | `Article` oder `BlogPosting` mit `headline`, `datePublished`, `dateModified` und `author` — das E-E-A-T-Signal, auf das Antwortmaschinen achten |
| **9. Titel und Beschreibungen** | `IS06` (17 %), `IS11` (2 %), `IS03` (96 %), `IS02` (90 %), `IS09` (96 %), `GE35` (67 %) | nur 27 von 158 Titeln nennen Ort, Zahl oder Nutzen; 3 von 158 Beschreibungen enthalten eine Handlungsaufforderung; drei Titel doppelt vergeben; im Einstieg der Startseite fehlt der **Ort** | Eine Änderung je Vorlage wirkt auf Dutzende Seiten — das beste Verhältnis von Aufwand zu Wirkung im ganzen Katalog. **`IS06` ist am 11.09.2026 als Ausnahme eingetragen** (siehe „Bewertung der Messpunkte“): Die Regel erkennt als Ort nur „Lenzing“ und als Nutzen eine feste Wortliste (Prüfwerkzeug `cockpit/pruefer/katalog/r_seo_inhalt.py:60–63`, `:105–107`). Die gemeldeten Titel nennen ihre Stadt, ihren Nutzen in eigenen Worten, oder lassen beides aus Gründen weg, die die Projektregeln vorgeben (Einrichtungsseiten ohne Festpreis, Rechtstexte). Offen bleiben in diesem Hebel `IS11`, `IS03`, `IS02`, `IS09` und `GE35` |
| **10. Bilder und Ladeverhalten** | `PF16` (0 %), `PF18` (0 %), `PF19` (33 %), `PF17` (69 %), `VL15` (76 %), `VL16` (67 %) | 0 von 340 Bildern mit `srcset`; 158 von 158 Seiten ohne `fetchpriority=high` am ersten Bild; 316 Bilder weder lazy noch als LCP-Bild ausgezeichnet; kein Critical CSS; `/`, `/en/`, `/ro/` über 120 KiB HTML **`PF18` ist am 07.09.2026 dort gebaut, wo er zutrifft: auf `/ueber-uns/`. Auf den übrigen Seiten ist das erste Bild im `main` dasselbe 44-px-Dekobild unten in der Anfragekarte — es hoch zu priorisieren zöge es vor den sichtbaren Inhalt. Am 10.09.2026 als Ausnahme eingetragen** (siehe „Bewertung der Messpunkte"): Hohe Ladepriorität tragen das Hero-Porträt (`templates/index.html:69`), das Porträt auf `/ueber-uns/` (`templates/ueber_uns.html:48`) und das erste Referenzbild (`templates/referenzen.html:43`) — mal drei Sprachfassungen genau die Seiten, die die Messung als bestanden zählt. **`PF17` ist am 10.09.2026 aus demselben Grund als Ausnahme eingetragen** — er trifft dasselbe eine Bild von der anderen Seite: Es eifrig zu laden hiesse, ein Dekobild am Fuss eines Formulars vor den sichtbaren Inhalt zu ziehen. Die zweite Hälfte des Rats ist erfüllt: Jedes Bild, das im `main` nach dem ersten steht, lädt verzögert; eifrig geladen wird nur, wo es ein echtes LCP-Bild gibt. Offen bleiben in diesem Hebel `PF16`, `PF19`, `VL15` und `VL16` | siehe [70-PERFORMANCE.md](70-PERFORMANCE.md) „Offen" |
| **11. Konversion** | `KV07` (0 %), `KV06` (0 %), `KV05` (1 %), `KV04` (50 %), `KV09` (33 %), `KV14` (98 %) | keine Danke-Seite; Formulare mit durchschnittlich 6,6 Feldern, Ausreisser `/` mit 37 und `/angebot/` mit 40 Feldern (das sind die Auswahlfelder des Konfigurators, kein klassisches Anfrageformular); 2 von 6 Vertrauenssignalen; drei Leistungs-Hubs ohne Nutzenpunkte und Zeitzusage im Angebotsblock | Danke-Seite bauen, Datenschutzsatz unter jedes Formular, Angebotsblock auf den Hubs vervollständigen |
| **12. Themenbereiche und Ortsseiten** | `SU08` (77 %), `SU05` (0 %), `VL12` (67 %) | drei Bereiche mit nur einer Seite (`/referenzen/`, `/it-notfall/`, `/it-sicherheit-test/`); „0 Ortsseiten, Zielgrösse 8" — das Werkzeug erkennt das Präfix `/it-service/` nicht als Ortsseiten, es gibt **sieben** | `/referenzen/` mit Fallstudien ausbauen; die beiden Werkzeugseiten sind bewusst einzeln; die Ortsseiten-Erkennung im Werkzeug nachziehen, statt Seiten zu bauen. **`SU05` ist am 12.09.2026 als Ausnahme eingetragen** (siehe „Bewertung der Messpunkte"): Der Befund dieses Tages zählt laut Commit **sieben** Ortsseiten gegen eine Zielgrösse von acht, erkennt das Präfix also inzwischen — die achte entsteht trotzdem nicht. Weitere Ortsseiten sind in `../docs/SEO-AUSBAU-3.md` ausdrücklich als Doorway-Pages verworfen (Regel A16, und die steht auf einer Messung), und den echten Auftrag aus einem achten Ort, den der Rat selbst verlangt, gibt es nicht. Offen bleiben in diesem Hebel `SU08` und `VL12` |
| **13. Barrierefreiheit** | `BF18` (50 %), `BF19` (0 %), `BF21` (96 %) | Kontrast-Einzelprüfung 0 % mit 32 betroffenen Elementen (Antippziele 100 %); `prefers-reduced-motion` nicht gefunden, obwohl in `static/css/style.css` Zeile 500 vorhanden; 6 Seiten teilen sich einen Titel | **Der Kontrastteil ist am 12.09.2026 abgearbeitet, in drei Schritten:** 32 Elemente (Messung 02.09.) → eine falsche Textfarbe korrigiert, 15 (06.09., `--ink-dim`) → **ein** beanstandetes Element (Messung 12.09.), und das ist behoben: `.err-code` auf der 404- und der 500-Seite trug `opacity:.5` über `--accent` und kam damit auf 2,94:1, ohne die Deckkraft hält dieselbe Farbe 8,41:1. Die **Antippziele** bestanden die Einzelprüfung schon in allen drei Messungen (100 %). **Noch am 12.09.2026 kam ein vierter Schritt dazu, den keine Messung verlangt hatte:** Weil das gemeldete Element bereits geheilt war, wurde stattdessen die Regel geprüft, die der Kopf von `style.css` seit dem Umbau 2026-08 aufstellt — Gold als Text auf Hell nur über `--accent-ink`. Zwei Regeln hielten sich nicht daran, `.marquee-track i` mit laut Commit 1,69:1 und `.rg-km` mit 2,02:1, beide dauerhaft sichtbar und ausserhalb jedes `on-dark`; beide tragen jetzt `--accent-ink`, und `GoldAlsTextTest` verhindert den Rückfall. **Noch am selben Tag ein fünfter Schritt — die drei Zustände, die der vierte ausdrücklich hatte liegen lassen:** `.rg-sw-link:hover strong` und `:focus-visible strong`, `.fld-recht a:hover` und `.ub-fakten a:hover` färbten Text erst auf Eingabe golden und standen deshalb in keiner Einzelprüfung; gemessen sind das laut Commit dieselben 2,02:1 auf Weiss wie bei den dauerhaften Fällen, und `:focus-visible` ist der Zustand, in dem eine Tastaturbedienung **dauerhaft** steht. Alle drei tragen jetzt `--accent-ink`, die Ausklammerung der Zustände im Test ist entfallen (Suite 298). Was die Regel danach noch meldet, zeigt erst der nächste Lauf. Offen bleiben in diesem Hebel `BF19` — eine Werkzeugfrage, `prefers-reduced-motion` steht im Quelltext (siehe [90-NOTIZEN.md](90-NOTIZEN.md) Nr. 4) — und `BF21`, die sechs Seiten mit demselben Titel |

## Beim Kunden

Braucht Zuarbeit von Florin Feier — **nicht am Rechner lösbar, nicht darauf warten.**

> **Nachgeprüft am 06.09.2026, alle vier bestätigt.** Die Domain hat weiterhin
> **keinen SPF-, keinen DKIM- und keinen DMARC-Eintrag** (DNS-Abfrage gegen 8.8.8.8);
> `http://wvm-it.tech/` antwortet mit **200 und der Parkseite** von domaintechnik.at,
> `https://wvm-it.tech/` baut keine Verbindung auf. In den Railway-Variablen steht als
> Absender unverändert `WVM-IT <enigmabible1@gmail.com>`.
>
> **Warum die Absenderadresse in diesem Durchgang nicht umgestellt wurde:** Ein
> Wechsel auf `support@wvm-it.tech` ohne passenden SMTP-Zugang bricht den Versand —
> Gmail sendet nur als authentifizierter Nutzer oder verifizierter Alias. Das wäre
> ein stiller Totalausfall aller Anfragen gewesen. **Teilmaßnahme am 06.09.:**
> Reply-To zeigt jetzt auf `support@wvm-it.tech` bzw. auf den Interessenten.

### Vier Angaben, die `KV09` und `GE11` zusammen schliessen (06.09.2026)

Die Messung meldet fehlende Vertrauenssignale (`KV09`: Bewertungen, Erfahrung mit
Jahreszahl, Absicherung) und `sameAs`-Verweise (`GE11`: „die Seite steht als Insel
da"). Beides ist **am Rechner nicht lösbar** — die Projektregel verbietet erfundene
Bewertungen, Zertifikate, Partnerlevel und Kundenzahlen, und eine geratene Zahl wäre
für die Firma gefährlicher als der offene Befund. Der Schema-Knoten ist vorbereitet
und rendert, sobald das Feld gefüllt ist.

| Feld in `content.json` | Was gebraucht wird | Schliesst |
|---|---|---|
| `seit_jahr` | Seit wann WVM-IT arbeitet — die stärkste der vier, ein Jahr genügt | `KV09` |
| `profile` | Echte Profil-Adressen: Google-Unternehmensprofil, LinkedIn, WKO Firmen A–Z, Herold.at | `KV09`, `GE11` |
| `uid` | UID-Nummer, falls vorhanden | `KV09` |
| — | Betriebshaftpflicht: ob und bei wem — der Punkt „Absicherung" | `KV09` |

### Zwei Pflichtangaben fürs Impressum (07.09.2026, aus `EIG02`)

§ 5 Abs. 1 ECG verlangt beides; beides kann nur Florin bestätigen, weil es Tatsachen über sein Gewerbe sind und keine Formulierungsfrage. **Der Ausbau des Impressums am 12.09.2026 (`IS19`) hat daran nichts geändert und sollte es auch nicht:** Er hat nur ergänzt, was sich im Projekt belegen liess — die Blattlinie nach § 25 Abs. 4 MedienG, den für den Inhalt Verantwortlichen, das Tätigkeitsgebiet, Linkhaftung und Urheberrecht. Die zwei Angaben unten sind weiter die einzige Lücke, die das Impressum noch hat.

| Was | Warum | Wo es hingehört |
|---|---|---|
| **Wirtschaftskammer** — Landeskammer und Fachgruppe (vermutlich WKO Oberösterreich; Fachgruppe UBIT oder Gewerbe, je nach Gewerbeschein) | Z 6: die Kammer, der der Diensteanbieter angehört. Jeder österreichische Gewerbetreibende ist Pflichtmitglied — die Angabe fehlt also nicht, weil es sie nicht gibt, sondern weil sie nie eingetragen wurde | `content.json` → `impressum` |
| **Berufsbezeichnung und GISA-Zahl** (die Zahl ist nach ECG nicht zwingend, macht die Angabe aber überprüfbar) | Z 7: die Berufsbezeichnung und der Staat, in dem sie verliehen wurde | `content.json` → `impressum` |
>
> Zu Punkt 1 kommen die Kanäle aus `../docs/STRATEGIE-2026-09.md` §6, allen voran
> **huddlex.at** — die B2B-Anfrageplattform der WKO Oberösterreich, für die Florin
> seit dem 10.06.2020 als UBIT-Mitglied berechtigt ist, ohne sie zu nutzen.

| # | Punkt | Warum es bei ihm liegt | Wo die Vorlage liegt |
|---|---|---|---|
| 1 | **Google-Unternehmensprofil anlegen und verifizieren** | Öffentlicher Eintrag über sein reales Unternehmen, Verifizierung per Postkarte an seine Anschrift (5–14 Tage). Der wichtigste offene Punkt überhaupt: für die lokale Suche der entscheidende Hebel, **166 URLs gleichen sein Fehlen nicht aus**. Mit Profil erste Anrufe in 1–4 Wochen, ohne Profil lokal nichts | fertige Angaben in `../docs/SEO-KONZEPT-DACH.md` §7 — reines Abtippen, keine Denkarbeit |
| 2 | **SPF- und DMARC-Eintrag** in der DNS-Zone | Am 28.08.2026 geprüft: die Domain hat **weder SPF noch DMARC**. Folge: Mails landen häufiger im Spam — auch die Eingangsbestätigungen an Kunden, die gerade angefragt haben; ausserdem kann jeder Fremde mit Absender `@wvm-it.tech` schreiben. 30 Minuten Arbeit, wirkt sofort. Dazu: Versand läuft über eine private Gmail-Adresse mit Anzeigename „WVM-IT" und sollte auf eine Adresse `@wvm-it.tech` mit DKIM umgestellt werden | fertige Einträge in `../docs/SEO-KONZEPT-DACH.md` §8.1; `p=none` zuerst, nach vier Wochen auf `p=quarantine` |
| 3 | **Apex-Domain `wvm-it.tech` auf Railway zeigen lassen** | Der A-Record zeigt auf `213.145.224.30`, die Parkseite des Registrars; Railway meldet `verified: false`, Zertifikat `ISSUING`, DNS `REQUIRES_UPDATE`. Am 02.09.2026 nachgeprüft: **HTTPS ohne `www` baut keine Verbindung auf, HTTP liefert die Parkseite mit Status 200.** Die kürzere Adresse ist die, die Leute tippen und die in Zitaten steht — solange sie nicht auflöst, ist jeder Verweis darauf ein Totlink, und Google sieht zwei Zustände derselben Marke. **Seit dem 12.09.2026 hängt ein zweiter Punkt daran:** HSTS mit `preload` (`SI03`) lässt sich erst einschalten, wenn diese Adresse auflöst — ein Eintrag in die Preload-Liste macht eine parkende Apex-Domain dauerhaft unerreichbar, und aus der Liste kommt man nur schwer wieder heraus (`config/settings.py:262–266`) | beim Registrar den A-Record durch einen CNAME auf `ibw105v9.up.railway.app` ersetzen (oder ALIAS/ANAME, falls am Apex kein CNAME erlaubt ist) |
| 4 | **UID-Nummer und Kammerzugehörigkeit** fürs Impressum | Beide sind nicht bekannt; die Felder in `content.json` sind vorbereitet und rendern, sobald sie gefüllt sind. Gewerbebehörde (BH Vöcklabruck) und Rechtsvorschrift stehen bereits | `content.json` → `uid`, `kammer` |
| 5 | **Gründungsjahr und Loxone-/KNX-Partnerstatus** | Starke Vertrauenssignale bei einem IT-Dienstleister; werden erst gerendert, wenn sie gefüllt sind. Beim Partnerstatus muss der Level dabeistehen (Loxone Silver/Gold, KNX-Partner) — nichts erfinden | `content.json` → `seit_jahr`, `partner_status` |
| 6 | **Erste echte Bewertungen einsammeln** — erst nach Freischaltung des Profils | Müssen von echten Kunden kommen. Drei erfundene Kundenstimmen standen hier schon einmal live und sind in AT/DE nach UWG angreifbar | `SEO-PLAN.md` T5 |
| 7a | **Die AGB gegenzeichnen** | Sie stehen seit dem 05.09.2026 live unter `/agb/`, damit der Deploy nicht blockiert — dasselbe Verfahren wie bei den zwölf Preisen am 28.08. Sie sind konservativ formuliert (österreichisches Recht, Haftung begrenzt, Abschnitt 9 sagt ausdrücklich keine Platzierung zu), aber **bis zu Florins Bestätigung sind sie eine offene Zusage, kein erledigter Punkt**. Er sollte sie einmal lesen, besonders Abschnitt 4 (Zahlungsziel 14 Tage), 6 (Kündigung zum Quartalsende) und 14 (Referenznennung) | `content.json` → `agb` |
| 7 | **Zwölf geschätzte Preise gegenzeichnen** | Am 28.08.2026 von Bastian freigegeben und live, damit der Deploy nicht blockiert — Florins Bestätigung steht weiter aus. Fällt eine Zahl, ändert sie sich nur in `landing/views.py::ANGEBOT_GROUPS`; danach meldet `pruefe_seite` jede Textstelle mit dem alten Wert | Liste in `../docs/RELAUNCH-START.md` §2a und `../docs/RELAUNCH-PLAN.md` §7 |
| 8 | **Bestandskunden persönlich ansprechen** (Aufhänger: der Beitrag zur Datensicherung) | Die Mail muss von Florin kommen, sonst trägt sie nicht. Laut `AKQUISE-SOFORT.md` der Kanal mit der höchsten Trefferquote, Wirkung in 3–7 Tagen — der Textvorschlag liegt vor | `../docs/AKQUISE-SOFORT.md` Kanal 2 |

## Erledigt

| Datum | Was | Beleg |
|---|---|---|
| **12.09.2026** | **Paket 193: ein Punkt zu Ende gebaut, einer nicht möglich, einer als Ausnahme belegt.** ⚠ **Liegt auf `sofort/2026-09-12-bf18-und-2-weitere`, nicht auf `main`** (`git log main..HEAD` nennt am 12.09.2026 neben den drei Commits von Paket 189 die zwei dieses Pakets). `BF18` — die Messung nennt unverändert `.err-code`, und das ist seit Paket 184 geheilt; **offen war, was Paket 189 ausdrücklich hatte liegen lassen:** drei Regeln setzen Gold als Text erst auf Eingabe und stehen deshalb in keiner Lighthouse-Einzelprüfung. Gemessen ist dort nichts anderes — `--accent` (`#d8a43d`) auf `--surface` (`#ffffff`), laut Commit **2,02:1** —, und `:focus-visible` ist kein flüchtiger Zustand, sondern der, in dem eine Tastaturbedienung dauerhaft steht. Geändert sind **drei Deklarationen** in `static/css/style.css`: `.rg-sw-link:hover strong` **und** `:focus-visible strong` (`:1851`), `.fld-recht a:hover` (`:2364`) und `.ub-fakten a:hover` (`:2387`), alle drei jetzt `--accent-ink` (laut Commit 5,52:1 auf derselben Fläche). `.on-dark .fld-recht a:hover` bleibt unberührt — dort steht derselbe Text auf dunklem Grund, `--accent2` löst zu `#eec77a` auf. **Die erste der drei reicht weiter, als der Commit sagt:** Der Verweis-Baustein `.rg-sw-link` steht nicht nur auf den sieben Regionsseiten, sondern in **fünf Vorlagen** — `region.html:82`, `begriff.html:61`, `beitrag.html:84`/`:101`/`:115`, `checkliste.html:89`/`:96`, `vergleich.html:96` —, und alle fünf setzen ihn in denselben weissen Kasten `.rg-schwerpunkt` (`style.css:1846`, `background:var(--surface)`); er trifft damit auch Glossar, Fachbeiträge, Checklisten und Vergleiche. `.fld-recht` steht über `templates/datenschutzhinweis.html:12` in **jedem** Anfrageformular, `.ub-fakten` in `templates/ueber_uns.html:54`. Im Test fällt die Ausklammerung der Zustände weg: `ZUSTAENDE` ist gelöscht, `test_gold_steht_nirgends_als_dauerhafte_textfarbe` heisst jetzt `test_gold_steht_nirgends_als_textfarbe` und prüft **alle** Regeln mit goldener Textfarbe; dazu `test_die_drei_zustaende_tragen_accent_ink`, das die drei Selektoren einzeln festhält und `.on-dark` ausnimmt. `GoldAlsTextTest` hat damit drei Prüfungen, die Suite **297 → 298** (am 12.09.2026 in `landing/tests/test_kontrast.py` nachgezählt) · `SI03` — **nicht möglich, keine Codezeile geändert.** `includeSubDomains` steht seit dem 05.09.2026 auf `True` (`config/settings.py:261`, per Umgebungsvariable abschaltbar, Begründung im Kommentar darüber); gemeldet ist damit allein `preload`, und das steht **mit Begründung** auf `False` (`:262–266`): Solange die Apex-Domain `wvm-it.tech` beim Registrar parkt (A-Record `213.145.224.30`, „Beim Kunden" Nr. 3), machte ein Eintrag in die Preload-Liste sie dauerhaft unerreichbar — und aus dieser Liste kommt man nur schwer wieder heraus. Der Punkt fällt in dem Moment, in dem Nr. 3 erledigt ist, und keine Sekunde früher; er steht seither als „beim Kunden" unter „Bewertung der Messpunkte", und in [10-TECHNIK.md](10-TECHNIK.md) ist dabei die **falsche Angabe berichtigt**, `includeSubDomains` und `preload` seien beide aus · `SU05` — **als Ausnahme belegt statt gebaut, keine Codezeile geändert**; Begründung unter „Bewertung der Messpunkte", die Folge für die Regionsseiten in [50-LOCAL-SEO.md](50-LOCAL-SEO.md), die von `BF18` in [20-DESIGN.md](20-DESIGN.md) | Commits `f722fda`, `c1b9aca` |
| **12.09.2026** | **Paket 189: zwei Punkte gebaut, einer als Ausnahme belegt.** ⚠ **Liegt auf `sofort/2026-09-12-bf18-und-2-weitere`, nicht auf `main`** (`git log main..HEAD` nennt am 12.09.2026 die drei Commits dieses Pakets). `BF18` — **das Element, das die Messung dieses Tages nennt, war schon geheilt**: `.err-code` verlor seine Deckkraft mit Paket 184, und die Messung stammt von der Live-Adresse, also von `main`. Offen war deshalb nicht der Befund, sondern die Frage, was er als Nächstes trifft — und die Antwort stand seit dem Umbau 2026-08 im Kopf von `style.css`: „Gold als Text auf Hell nur über `--accent-ink` — `#d8a43d` hat auf Weiss nur 2:1 Kontrast." **Geschrieben stand die Regel, geprüft wurde sie nie**, und zwei Regeln hielten sich nicht daran, beide ausserhalb jedes `on-dark` und beide ohne jede Eingabe sichtbar: `.marquee-track i`, der Trenner im Leistungsband der Startseite, mit `--accent` **und** `opacity:.75` (laut Commit angekommen als `#e1ba6c` auf `#fbfaf8`, **1,69:1** — derselbe Deckkraft-Fehler wie bei `.err-code`), und `.rg-km`, also Datum und Lesezeit auf `/aktuelles/`, Punktzahl auf `/checkliste/`, Entfernung und Fahrzeit auf `/it-service/`, mit `--accent` auf `#ffffff` (**2,02:1**). Geändert sind **zwei Deklarationen** in `static/css/style.css`: beide auf `--accent-ink`, die Deckkraft am Trenner entfällt — laut Commit hält das Token 5,52:1 auf `--surface` und 4,90:1 auf `--bg-2`, mit `opacity:.75` wären es 3,28:1, deshalb geht beides nur zusammen. Aufbau, Klassen, Kennungen und Schriftgrössen sind unberührt. Neu `GoldAlsTextTest` in `landing/tests/test_kontrast.py` mit zwei Prüfungen, die die **Verwendung** lesen statt des Tokens; sie liest die Eigenschaft, nicht die Zeichenkette (`accent-color` und `border-color` enden auf dieselben Buchstaben), und klammert `:hover`, `:focus`, `:active`, `:checked` aus. Drei Hover-Zustände bleiben deshalb bewusst auf Gold. Suite **297 Testfunktionen in 19 Dateien** (am 12.09.2026 nachgezählt), Gegenprobe laut Commit gefahren · `IS19` — die **eine** dünne Seite von 198 war `/impressum/`, und von den drei Wegen des Katalogs blieb nur der mittlere: Zusammenlegen geht bei einer Anbieterkennzeichnung nicht, und `noindex` nähme den einzigen Ort aus dem Index, an dem Anschrift, Gewerbebehörde und Verantwortlicher zusammenhängend stehen — bei einem Betrieb ohne Google-Unternehmensprofil die falsche Richtung. **Der grösste Fund war eine Lücke, die die Seite selbst aufgemacht hatte:** Sie beruft sich in ihrer ersten Zeile auf § 25 MedienG und nannte keine **Blattlinie**; die grundlegende Richtung nach § 25 Abs. 4 MedienG steht jetzt da und zählt die Leistungsfelder aus `landing/leistungen.py` auf. Dazu der für den Inhalt Verantwortliche, das Tätigkeitsgebiet und die ausgeschriebenen Absätze zu Linkhaftung und Urheberrecht — **jede Angabe mit Beleg im Projekt, keine geraten**; UID und Kammer fehlen weiter und bleiben unter „Beim Kunden". Geändert ist **ein Feld in `content.json`**, die Vorlage bleibt unberührt (`templates/recht.html:39`, `|linebreaks`): laut Commit **152 → 337 Wörter**, und `seo_bericht` meldet in der Zeile „Auffällig" zum ersten Mal „Nichts". **Nachgebessert am selben Tag:** Die erste Fassung setzte die neuen Angaben in vier eigene Absätze, und `|linebreaks` macht aus jeder Leerzeile ein `<p>` — die Designwache meldete auf `/impressum/` 291 → 295 Elemente und an Stelle 174 ein `<p>` statt eines `<br>`. Der Text steht jetzt in den **vorhandenen neun Absätzen** (Blattlinie und Stand im Offenlegungsabsatz, Verantwortlicher und Tätigkeitsgebiet beim Unternehmensgegenstand); Wortzahl und Inhalt bleiben, der Aufbau ist wieder der von vorher. Merksatz: In einem `|linebreaks`-Feld ist eine Leerzeile ein Element, und ein Element ist Aufbau. `landing/stand.py` ist dabei **ausnahmsweise von Hand** auf den Wert gesetzt worden, den `stand_schreiben` errechnet (in der Sitzung nicht ausführbar, ohne Nachzug meldet `--pruefen` im CI-Lauf Rückgabewert 1); weil alle vier Rechtsseiten an `templates/recht.html` und `content.json` hängen, tragen auch Datenschutz, AGB und Barrierefreiheitserklärung den 12.09.2026 ohne Inhaltsänderung · `PF13` — **als Ausnahme belegt statt gebaut, keine Codezeile geändert**; Begründung unter „Bewertung der Messpunkte", die von `BF18` in [20-DESIGN.md](20-DESIGN.md) und [10-TECHNIK.md](10-TECHNIK.md), die von `IS19` in [30-INHALTE.md](30-INHALTE.md), die von `PF13` in [70-PERFORMANCE.md](70-PERFORMANCE.md) — dort ist zugleich eine **falsche Angabe berichtigt**: Die Datei behauptete „Hash im Namen" und „WhiteNoise mit Manifest-Storage", beides trifft nicht zu | Commits `1d8edb1`, `5649ec8`, `b2d2cd0` |
| **12.09.2026** | **Paket 184: ein Punkt gebaut, zwei als Ausnahme belegt.** **Ist auf `main`** — `git branch --contains 1675108` nennt am 12.09.2026 `main` (der Vermerk „liegt nur auf dem Zweig" entstand davor). `BF18` — die Messung dieses Tages führte **ein** Element mit zu geringem Kontrast, und es war der Kandidat, der unter „Offen" Nr. 5 seit dem 07.09. stand: `.err-code`, die grosse Zahl auf der 404- und der 500-Seite. **Falsch war nicht die Farbe, sondern die Deckkraft darüber.** `--accent` (`#d8a43d`) hält auf dem dunklen Grund `#12100c` 8,41:1; die Regel setzte zusätzlich `opacity:.5`, und Deckkraft ist keine Eigenschaft des Textes, sondern eine Anweisung, ihn mit dem Grund zu verrechnen — an kam effektiv `#755a24`, und das sind **2,94:1**. Damit riss die Zahl selbst die 3:1, die grossem Text zugestanden werden. Geändert ist **eine Deklaration**: `opacity:.5` entfällt, Farbe, Schriftgrad und Aufbau bleiben unberührt (`static/css/style.css`). Warum es keiner Prüfung auffiel: `test_kontrast.py` liest die **Token**, und die halten nachweislich alle ihre 4,5:1 in beiden Fassungen — eine Deckkraft unter 1 ist aber eine Farbänderung, die dort nicht ankommt. Neu deshalb `ErrCodeKontrastTest` mit drei Prüfungen, die **die Mischung nachrechnen** statt des Tokens, den dunklen Kopf in beiden Vorlagen festhalten (die Rechnung gilt nur, solange er dunkel ist) und die Mischfunktion gegenprüfen; laut Commit ist die Gegenprobe gefahren — mit `opacity:.5` meldet der Test 2,94:1 und schlägt fehl. Suite **292 → 295 Testfunktionen** (am 12.09.2026 nachgezählt), `pruefe_seite` laut Commit grün über 198 URLs. Der Eintrag unter „Offen" Nr. 5 ist zugleich **berichtigt**: Er rechnete gegen hellen Grund (1,4:1), tatsächlich steht die Zahl in einem `on-dark`-Kopf · `VL21` und `VL13` — **als Ausnahme belegt statt gebaut, keine Codezeile geändert**; beide Begründungen unter „Bewertung der Messpunkte", die von `VL21` auch in [10-TECHNIK.md](10-TECHNIK.md) („Fallen", Änderungsdaten), die von `BF18` in [20-DESIGN.md](20-DESIGN.md) | Commits `1675108`, `8bb52aa`, `c5d2094` |
| **11.09.2026** | **Paket 170: ein Punkt anders gebaut als vorgeschlagen, zwei als Ausnahme belegt.** **Ist auf `main`** — der Merge `0642f58` liegt noch auf demselben Tag, `git branch --contains d87313f` nennt am 12.09.2026 `main` (der Vermerk „liegt nur auf dem Zweig" entstand davor). `PJ11` — der Rat lautete „jede Zeile mit `==` und ein Lockfile daneben“. Beides stand schon: `requirements.txt` nennt fünf Pakete mit fester Fassung, `requirements.lock` liegt seit dem 05.09.2026 im Projekt. **Die Lücke war eine andere:** Das Lockfile galt nur im CI-Lauf (`.github/workflows/pruefen.yml:63`, `pip install -r requirements.lock`). Railway baut mit Nixpacks (`railway.json`), und Nixpacks installiert nur aus `requirements.txt`. Die mittelbaren Pakete (asgiref, sqlparse, tzlocal, pytz, six, packaging, tzdata) kamen also bei jedem Deploy in der Fassung, die an dem Tag neu war. Jetzt steht in der letzten Zeile von `requirements.txt` `--constraint requirements.lock`: pip hält sich an die Fassungen aus dem Lockfile, installiert aber kein Paket, das die fünf direkten nicht brauchen. Weicht das Lockfile von `requirements.txt` ab, bricht der Bau ab, und das ist gewollt. Dass die Datei beim Bau daneben liegt, ist im Kopfkommentar begründet: Für `requirements.txt` setzt Nixpacks kein `only_include_files`, und `.railwayignore` schliesst `requirements.lock` nicht aus. Im Kopf des Lockfiles löst der Erzeugungsbefehl jetzt gegen eine Kopie **ohne** die Constraint-Zeile auf, sonst bliebe er an den alten Fassungen hängen. Beide Dateien enthalten nur noch ASCII, weil pip sie auf dem Bauserver mit dessen Zeichenkodierung liest. Geprüft laut Commit mit pips eigenem Parser und der Suite. **Ein echter Railway-Bau mit der neuen Zeile war am 11.09. noch nicht gelaufen**; der Merge auf `main` ist inzwischen erfolgt, im Build-Log nachgesehen hat noch niemand (siehe [10-TECHNIK.md](10-TECHNIK.md), „Offen“). Die Messregel bewertet das Lockfile weiterhin nicht: `r_projekt.py:365–371` sucht es in der Dateiliste des Code-Audits, und die liest nur die Endungen aus `codeaudit.py:47–52`, `.lock` gehört nicht dazu. Das liegt am Werkzeug, nicht an der Seite · `VL01` und `IS06` — **als Ausnahme belegt statt gebaut, keine Codezeile geändert**; beide Begründungen unter „Bewertung der Messpunkte“, die von `VL01` auch in [10-TECHNIK.md](10-TECHNIK.md), die von `IS06` in [40-SEO.md](40-SEO.md) und [30-INHALTE.md](30-INHALTE.md) | Commits `d87313f`, `a2f1196`, `2d6061b` |
| **10.09.2026** | **Paket 153: ein Punkt gebaut, zwei als Ausnahme belegt.** `GE26` — vier Seiten benannten ihren Kernbegriff, ohne ihn je zu definieren. Jetzt sagt `/kontakt/`, was **Fernwartung** ist: ein Zugriff über eine gesicherte Verbindung statt einer Anreise, in einer Sitzung mit Zustimmung, jederzeit abbrechbar, und ohne neue Freigabe kommt niemand zurück auf das Gerät. `/einrichten/` sagt, was ein **Festpreis** ist: ein vorher genannter Betrag je Gerät oder Vorgang, ohne Vertrag und ohne laufende Betreuung, der die per Fernwartung erledigte Arbeit deckt — wird ein Einsatz vor Ort nötig, wird das vorher gesagt und mit 120 € je Stunde zzgl. Anfahrt abgerechnet; **die Zahl kommt aus dem Katalog, keine ist neu**. Damit steht dort auch begründet, warum der Preis im Einrichtungs-Silo ohne „ab" auskommt. `/leistungen/server-datensicherung/` sagt, was eine Sicherung zur Sicherung macht (geprüft: jeder Lauf kontrolliert, Wiederherstellung regelmässig getestet; getrennt vom Server aufbewahrt) — beide Bedingungen standen vorher schon in `kurz` und in der Problemliste, nur nie als Satz. `/leistungen/ki-automatisierung/` sagt, dass **KI-Automatisierung** kein Produkt ist, sondern ein Ablauf, und was bewusst bei einem Menschen bleibt. Der Satz steht je **im vorhandenen Absatz** — kein neues Element, kein neuer Link, keine neue Überschrift; geändert sind ausschliesslich Zeichenketten in `landing/i18n/{de,en,ro}.py`, `einrichten_{de,en,ro}.py` und `seiten_{de,en,ro}.py`, also vier Basis-Pfade mal drei Sprachen. **Die vier Rechtstexte bewusst nicht:** Impressum, AGB, Datenschutz- und Barrierefreiheitserklärung sind Zusagen, und ein Definitionssatz wäre dort eine Aussage ohne Deckung. `pruefe_seite` meldet 198 URLs und 27 Preiszahlen in Ordnung, die Suite läuft durch · `PF17` und `IS18` — **als Ausnahme belegt statt gebaut, keine Codezeile geändert**; beide Begründungen unter „Bewertung der Messpunkte", die von `PF17` auch in [70-PERFORMANCE.md](70-PERFORMANCE.md), die von `IS18` in [30-INHALTE.md](30-INHALTE.md) | Commits `228f726`, `faf3716`, `024204f` |
| **10.09.2026** | **Paket 147: ein Punkt gebaut, zwei als Ausnahme belegt.** `PJ03` — der Punkt war seit dem 07.09.2026 gebaut und wurde trotzdem weiter als offen gemeldet; der Grund lag **nicht** in den Tests, sondern im Import-Block von `landing/tests/test_module.py`. Die Modulnamen standen dort gebündelt in Klammern (`from landing import (branchen, glossar, …)`). Python bindet dabei genau dasselbe, eine Quelltext-Analyse sieht aber nur das Paket vor dem `import` — sie liest „`landing` angefasst", nicht „`branchen` angefasst". Jetzt steht **ein Modul je Zeile**, mitsamt der Begründung im Kommentar darüber, damit der nächste Durchgang die Klammer nicht aus Ordnungsliebe wiederherstellt. Die Klammer hatte nicht die Prüfung geschwächt, sondern den Beleg dafür, dass es sie gibt. Beim Durchgehen fielen drei Lücken auf und sind geschlossen: `landing/middleware.py` war überhaupt nicht importiert — die vier reinen Funktionen der Sprachweiche (`_is_default_page`, `_sprache_aus_pfad`, `_has_lang_prefix`, `_accept_language`) hatte kein Test einzeln angefasst, obwohl genau dort der teuerste Fehler dieser Seite stand (bis 05.09.2026 galt jede präfixlose Adresse als Startseite); die neue `SprachweicheTest` prüft sie mit fünf Funktionen, die letzte davon **strukturell über `i18n.LANGS`**, also ohne Anfassen, wenn eine vierte Sprache dazukommt. Dazu drei bis dahin ungeprüfte Funktionen aus `landing/supa.py` im Fall ohne Zugang: `set_newsletter_run_count`, `job_status` und `claim_newsletter_run` — Letzteres mit eigener Prüfung, weil es die Sperre gegen zwei Newsletter je Woche ist und ohne Datenbank `False` liefern muss, den Versand also im Zweifel **nicht** laufen lässt. Damit **282 Testfunktionen in 18 Testdateien** (vorher 276; am 10.09.2026 nachgezählt) · `PF18` und `PJ07` — **als Ausnahme belegt statt gebaut, keine Codezeile geändert**; beide Begründungen unter „Bewertung der Messpunkte", die von `PF18` auch in [70-PERFORMANCE.md](70-PERFORMANCE.md) | Commits `3b18af8`, `ff9c9f4`, `556c813` |
| **08.09.2026** | **Paket 138 — abgewiesen, die Arbeit liegt auf dem Zweig.** ⚠ **Liegt auf `sofort/2026-09-08-ge23-und-2-weitere`, nicht auf `main`** (nachgemessen am 10.09.2026: `git branch --contains 87f7dc3` nennt nur diesen Zweig, `templates/recht.html` bindet auf `main` kein `antwort.html` ein). Das Paket wurde abgewiesen, weil der Absatz auch auf `/impressum/` und `/datenschutz/` erschien; die Entscheidung darüber steht aus. Was gebaut wurde: ein Punkt gebaut, einer als Ausnahme belegt, einer nicht lösbar. `GE23` — Impressum, Datenschutz-, Barrierefreiheitserklärung und AGB tragen den Antwortabsatz aus `templates/antwort.html` jetzt direkt unter der `h1`, in derselben Form und an derselben Stelle wie `/kontakt/` und `/ueber-uns/`. Bis dahin begann jede der vier mit ihrer Formzeile — „Offenlegung gemäß § 5 ECG und § 25 MedienG.", neun Wörter, die die Frage der Überschrift nicht beantworten. Der Text steht in `landing/i18n/de.py` unter `recht.<art>_kurz` und **fasst nur zusammen, was in `content.json` darunter ohnehin steht**: keine neue Angabe, kein Preis, keine zweite Anschrift — Inhaber und Sitz kommen über `{inhaber}`/`{adresse}` aus `content.json` (`views._rechtsseite`), dieselbe Bauweise wie bei `ueber.kurz`. Die englischen und rumänischen Adressen erben den **deutschen** Absatz aus `de.py` und zwar bewusst: Der Rechtstext darunter ist in allen drei Sprachen deutsch (`IS21`), und `/en/impressum/` wie `/ro/agb/` tragen ohnehin `noindex` mit `canonical` auf die deutsche Fassung — ein Test hält die Gleichheit fest, damit daraus keine englische Zusammenfassung über einem deutschen Rechtstext wird. Vier neue Testfunktionen prüfen genau die Bedingungen, an denen die Messung scheiterte: 15 bis 90 Wörter, darin eine Zahl mit Einheit oder ein Festlegungssatz, aufgelöste Platzhalter, kein Euro-Betrag. Auf dem Zweig damit **280 Testfunktionen**; **auf `main` sind es weiter 276 in 20 Dateien** (am 10.09.2026 nachgezählt). `landing/stand.py` für die vier Pfade nachgezogen · `MW18` — **als bewusste Ausnahme belegt statt gebaut, keine Codezeile geändert**; Begründung unter „Bewertung der Messpunkte" · `PJ16` („nichts Ungepushtes") — **in diesem Durchgang nicht lösbar:** Das Arbeitsverzeichnis ist sauber, aber die beiden Commits liegen noch nicht auf `origin`; Commit und Push macht das Werkzeug nach dem Doku-Durchgang, nicht die Bausitzung. **Nachbesserung am selben Tag, ohne eine Zeile Code:** Die Gegenprüfung hatte abgelehnt, weil sich in ihrer Sitzung kein Python starten liess und ein Urteil ohne gefahrenen Prüfbefehl eine Behauptung bleibt — `pruefe_seite` (198 URLs, „Alles in Ordnung"), `seo_bericht` (94 Basis-Pfade, einzige Auffälligkeit die seit je zu kurze Impressumsseite), `manage.py check` und die Suite (auf dem Zweig 280 Tests, OK) sind nachgefahren und grün. Dazu zwei Einträge im Bewertungsblock, die vorher nur als Fliesstext dastanden und damit für die Messung unsichtbar waren: `GE23` gegen die zwei Meldungen der Designwache (ein Element mehr an Stelle 161 — die einzig mögliche Wirkung des Punktes) und `PJ16` als nicht anwendbar | Commits `87f7dc3`, `b7293c2` |
| **08.09.2026** | **Davor am selben Tag, aus `../docs/PLAN-HARDWARE-2026-09-08.md`:** das Silo `/einrichten/` — acht Seiten plus Hub für „ein einzelnes Gerät, jetzt, ohne Vertrag" (`landing/einrichtungen.py`, darunter `arbeitsplatz`, `pc-tausch`, `windows-11`, `server`, `loxone`) —, die Vergleichsseite `/vergleich/pc-aufruesten-oder-neu-kaufen/` und der Fachbeitrag `/aktuelles/windows-10-ende-was-jetzt/`. Damit sind „Offen" Nr. 10 und 11 weg. Anschliessend die **Search Console von Hand bedient**, weil IndexNow nur Bing, Yandex und Seznam bedient und Google nicht: Sitemap-Index neu eingereicht (Status „Erfolgreich"), fünf Seiten einzeln zur Indexierung beantragt, alle fünf vorher auf „URL ist Google nicht bekannt" | Commits `f355374`, `6c3627f`, `8bb5b67`, `f578c73`, `7f784a8` |
| **07.09.2026** | **Paket 125: drei Punkte gebaut, zwei begründet abgelehnt.** `MW18` — die Sicherung vor dem Mailversand hängt jetzt an **allen** Anfragewegen; bis dahin lebten Kontaktformular, Angebots-Konfigurator, Richtangebot der Startseite und Kooperationsanfrage ausschliesslich in der E-Mail. Ein Django-Modell, das der Katalog rät, geht hier nicht: `DATABASES = {}`, `INSTALLED_APPS` führt weder `admin` noch `auth`. Gespeichert wird, was auch in der Mail steht — **keine IP** · `PF18` — das Porträt auf `/ueber-uns/` stand auf `loading="lazy"`, also eine Bremse genau vor dem wahrscheinlichen LCP-Bild, und trägt jetzt `fetchpriority="high"`; das 44-px-Dekobild der Anfragekarte bewusst **nicht**, siehe [70-PERFORMANCE.md](70-PERFORMANCE.md) · `PJ03` — `landing/tests/test_module.py` für die 32 Module, die kein Test berührte: Slugs und `NACH_SLUG`, jeder `leistung`- und `thema`-Verweis, Vollständigkeit je Sprache **am Modul statt über `get_pack`** (der Deep-Merge auf `de.py` verdeckt genau das), Selbsttest-Stufen, `stand.datum()`, `messung` ohne Kennung, `supa` als stiller No-Op ohne `WVM_DB_URL`, jeder eigene Management-Befehl ladbar und mit Hilfetext. Damit **252 Testfunktionen in 17 Dateien** (vorher 207). Abgelehnt: `PJ08` und `SI12`, Begründungen unter „Bewertung der Messpunkte". `stand_schreiben` lief in diesem Paket nicht; nachgerechnet ändert es nichts, weil von den geänderten Dateien nur `templates/ueber_uns.html` in die Datenquelle zählt (`anfrage_karte.html` ist ein Include und steht in keiner Liste) und `/ueber-uns/` in `landing/stand.py` schon auf `2026-09-07` steht | Commits `9b5993c`, `db983bf`, `ed3262c`, `455a413`, `c7c647f` |
| **06.09.2026** | **Zehn Punkte aus Werkzeug-Lauf #281.** `GE38` **397 HTML-Entities** aus den Sprachpaketen entfernt (`ro.py` allein 208 — die Diakritika standen als `&#259;`/`&#537;`/`&#539;`); im HTML sah das richtig aus, im JSON-LD stand für Google wörtlich `avocatur&#259;`. Zwei Pflaster in `views.py`, die zur Laufzeit auflösten, sind weg · `IS18` `/leistungen/` von **432 auf 1070 Wörter** in drei Sprachen: Arbeitsplatz, Hardware, Ablauf, fünf Fragen mit `FAQPage` · `BF18` Kontrast (siehe „Offen" Nr. 3) · `MW15` `manage.py pruefe_mail`, das sich **wirklich anmeldet** · `PF16` das Hero-Porträt lud 46 KB für 64 px, mit `fetchpriority=high` · `PJ05` das `except: pass` in `MessungMiddleware` meldet jetzt · `GE35` Firmenname im Einstieg · `GE23` Antwortabsatz auf `/angebot/` · `IS06` drei Titel. **197 Testfunktionen** (vorher 176) | `../docs/BEFUNDE-281-2026-09-06.md`; Commits `14017ce`, `cb2bda8` |
| **06.09.2026** | **Zwei Befunde geprüft und begründet abgelehnt.** `IS11` („88 Beschreibungen ohne Handlungsaufforderung"): `Cereți o ofertă.` **ist** die Aufforderung — die Regel kennt das rumänische Verb nicht; der Rest sind Rechtstexte, die zu Recht keine tragen. `KV09` (Vertrauenssignale): `seit_jahr`, `profile` und `uid` sind leer, und erfundene Bewertungen sind nach der Projektregel verboten — die vier nötigen Angaben stehen jetzt namentlich unter „Beim Kunden" | `../docs/BEFUNDE-281-2026-09-06.md` §8 |
| **05.09.2026** | **Search Console nachgezogen.** Der Sitemap-**Index** und alle vier Segmente einzeln eingereicht und binnen Minuten gelesen — `/sitemap.xml` **165**, `silos` 57, `leistungen` 39, `ratgeber` 35, `kern` 34, alle „Erfolgreich". Die Summe der Segmente entspricht exakt der Zahl aus `seo_bericht`: Sitemap, Prüfbefehl und Google zählen gleich. **Fünf neue Seiten zur Indexierung beantragt** (Veranstaltungstechnik, IT-Beratung, Über uns, AGB, Barrierefreiheit); `/anfrage/danke/` bewusst nicht, sie trägt `noindex`. Bei `/leistungen/it-beratung/` wies die URL-Prüfung schon `sitemap-leistungen.xml` als Fundstelle aus — die Segmentierung wirkt sofort in der Zuordnung, nicht erst in der Auswertung | `../docs/INDEXIERUNG.md` |
| **05.09.2026** | **Die Messung vom 04.09. nachgearbeitet — vier Punkte gebaut:** `PJ05` die **fünf verbliebenen verschluckten Ausnahmen** sichtbar gemacht (drei `reconfigure`-Blöcke fingen den Normalfall ab; ein kaputter JSON-LD-Block verschwand lautlos aus dem `seo_bericht`; `KanonischerHostMiddleware` schaltete die 301 auf die Hauptdomain ab, sobald `content.json` nicht lesbar war) · `GE23` **14 Glossareinträge und 4 Fachbeiträge** mit einer belegten Zahl im Antwortabsatz, jede Zahl aus dem Eintrag selbst oder aus `ANGEBOT_GROUPS`, keine neu · `SI16` auch `wvm_lang` auf `HttpOnly`, damit **beide** Server-Cookies gesperrt sind, und ein Test hält fest, dass kein Skript es liest · `SI08` die seit dem 05.09. **durchgesetzte** CSP durch fünf Prüfungen gegen stilles Verschwinden gesichert, statt einen zweiten Kopf zu setzen. Damit **130 Testfunktionen in neun Dateien** | Commits `fe88da4`, `a746f97`, `efe8d54`, `eb4354c`; [10-TECHNIK.md](10-TECHNIK.md), [40-SEO.md](40-SEO.md) |
| **05.09.2026** | **Ausbau September:** zwei neue Leistungsseiten (Veranstaltungstechnik, IT-Beratung) und vier fehlende Pflichtseiten (Über uns, AGB, Barrierefreiheitserklärung, Danke-Seite) — 158 → **165 URLs**. Titel und Beschreibungen **aller** Silos überarbeitet (27/158 → alle mit Ort, Zahl oder Nutzen; 3/158 → alle mit Handlungsaufforderung). Datenschutzhinweis und Honigtopf in allen zehn Anfrageformularen. **122 Testfunktionen** (vorher null) und ein CI-Lauf bei jedem Push. Durchgesetzte Content-Security-Policy mit Nonce, Permissions-Policy, HSTS mit `includeSubDomains`, `csrftoken` mit `HttpOnly`. Neun verschluckte Ausnahmen behandelt. Echte Änderungsdaten aus `landing/stand.py`, Sitemap in vier Segmenten, `WebPage`-Knoten auf jeder Seite, `Article` mit Autor auf 35 statt 15 Ratgeberseiten, alle `@id`-Verweise lösen auf. Atom-Feed unter `/feed/`. Startseite 211 → **183 KB** | `../docs/AUSBAU-2026-09.md` |
| **05.09.2026** | **Zwei Funde, die in keinem Plan standen:** Die 47 nur-deutschen Seiten trugen **94 hreflang-Verweise auf Adressen, die mit 404 antworten** — Google verwirft eine solche Gruppe vollständig. Und die Sprachumleitung galt für **jede** präfixlose Adresse statt nur für die Startseite: Wer einmal auf `/en/` war, wurde beim Klick auf einen deutschen Link zurückgeworfen. Dazu die 82 über interne Links unerreichbaren Seiten (`TS23`): Der Sprachumschalter lief über `/sprache/<lang>/`, das in `robots.txt` gesperrt ist | `../docs/AUSBAU-2026-09.md` §3 |
| **29.08.2026** | **SEO-Ausbau 3 abgeschlossen: 56 von 56 Aufgaben.** Aus 87 wurden **158 URLs** (76 Basis-Pfade, 114.641 Wörter, 0 verwaiste Seiten): Branchen-Silo (21 URLs), zehn weitere Fachbeiträge, Vergleiche (12), Kostenrechner, Sicherheits-Selbsttest, Notfallseite, Glossar (15), Checklisten (4), eigene 404-/500-Seite, interne Suche, Schema-Ausbau, `seo_bericht`. 17 Commits, Railway-Deploy nach rund 20 Sekunden live, 158 URLs an IndexNow mit HTTP 200 | `../docs/SEO-AUSBAU-3.md` Protokoll |
| 29.08.2026 | Drei Funde ausserhalb jedes Plans behoben: HTML war **unkomprimiert** (Startseite 204 → 35 KB, Unterseiten ~50 → ~11 KB), **Hero-Preload** stand auf 139 Seiten für ein Bild, das 138 davon nicht haben, **`/angebot/` hatte gar kein JSON-LD** | `../docs/SEO-AUSBAU-3.md` §11 |
| 29.08.2026 | Vier neue Prüfungen in `pruefe_seite`: Listenlängen je Sprache, Glossar-Mindestumfang 250 Wörter, verwaiste Seiten (9 → 0), Schema-Vollständigkeit. `docs/DEPLOY.md` angelegt (`123d4a7`) | `../CLAUDE.md` |
| 29.08.2026 | Firmensitz Lenzing an neun Stellen (F3), sieben Regionsseiten (A16), fünf Fachbeiträge mit `Article`-Schema (T1), `llms.txt` und `llms-full.txt` nachgezogen (G9), Preisprüfung über alle Seiten (G10), `SEO-KONZEPT-DACH.md` und `AKQUISE-SOFORT.md` geschrieben | `../docs/AUSBAU-2026-08.md` |
| 29.08.2026 | Mailversand belegt (echte Testanfrage, Railway-Log), fehlende **Eingangsbestätigung** des ausführlichen Kontaktformulars ergänzt (drei Sprachen) | `../docs/AUSBAU-2026-08.md` P4 |
| 28.08.2026 | **Relaunch live:** Positionierung auf EDV/IT gedreht, elf Leistungsseiten plus Hub, Kosten-, Referenzen-, Kontakt-, Impressums- und Datenschutzseite in DE/EN/RO — 57 URLs. Drei **erfundene Kundenstimmen entfernt**, Google Ads als buchbare Leistung ergänzt, Anrede vereinheitlicht | `../docs/RELAUNCH-START.md` |
| 28.08.2026 | **Umbau live** (Commit `60d3064`): helle Basis mit dunklen Bändern, vier Kontaktwege über der Falz, Hero-Widget mit zwei Reitern, Kurzformular je Leistungsblock über einen Endpunkt, sichtbare Preise mit Stand-Datum, FAQ, Kontrast auf ≥ 4,5:1; 48 von 50 Aufgaben | `../docs/UMBAU-START.md` |
| 28.08.2026 | **Sicherheit** (P2): Rate-Limiting auf alle fünf Formulare statt auf eins, Honeypot überall, Feldlängen begrenzt, X-Forwarded-For-Lücke geschlossen, Prüfbefehl `pruefe_sicherheit` | `../docs/AUSBAU-2026-08.md` |
| 28.08.2026 | **Duplikat-Host geschlossen** (F2): `wvm-it-shop.up.railway.app` leitet mit 301 auf die Hauptdomain um (am 02.09.2026 nachgeprüft) | `../docs/SEO-PLAN.md` |
| 28.08.2026 | **Search Console eingerichtet** (Property `https://www.wvm-it.tech/`, Konto `bastian.scherzinger05@gmail.com` — am 03.09.2026 nachgeprüft), Sitemap neu eingereicht, sechs URLs zur Indexierung beantragt, **Nullmessung** in `docs/seo/BASELINE.md` festgehalten; IndexNow eingerichtet und ausgelöst (HTTP 202) | `../docs/INDEXIERUNG.md` |
| 27.08.2026 | Design-System festgelegt (Tokens, Typografie, Komponentenregeln), `UMBAU-PLAN.md` und `SEO-PLAN.md` angelegt | `../docs/UMBAU-PLAN.md` §2 |

## Bewertung der Messpunkte

<!-- bewertung:anfang -->
| Punkt | Zustand | Grund | seit |
|---|---|---|---|
| TS11 | beim Kunden | Die Nebenadresse `https://wvm-it.tech` baut keine Verbindung auf, weil der A-Record der Apex-Domain auf die Parkseite des Registrars zeigt (213.145.224.30) und Railway `verified: false` meldet — sie erreicht die Anwendung also gar nicht, und eine 301 lässt sich im Code nicht setzen, wo keine Anfrage ankommt; jeden Host, der die Anwendung erreicht, leitet `landing.middleware.KanonischerHostMiddleware` bereits dauerhaft auf `www.wvm-it.tech` um (nachgeprüft am 28.08.2026 an `wvm-it-shop.up.railway.app`). Offen ist der CNAME beim Registrar, siehe „Beim Kunden“ Nr. 3. | 2026-09-05 |
| PJ02 | nicht anwendbar | Der Befund „keine einzige Testfunktion im Projekt“ stammt aus der Messung vom 02.09.2026; seit dem 05.09.2026 stehen **130 Testfunktionen** in `landing/tests/` (neun Dateien, `python manage.py test landing.tests`, rund 16 Sekunden), sie laufen bei jedem Push über `.github/workflows/pruefen.yml` und liegen damit weit über der Zielgrösse 30 des Katalogs. Die Zielbilder des Rats sind erfüllt: `test_urls.py` prüft jede URL auf 200, `test_preise.py` die Preislogik gegen `ANGEBOT_GROUPS`, `test_struktur.py` die Slugs. | 2026-09-05 |
| TS19 | bewusst so | Die Bild-Erweiterung ist bereits eingebaut — `sitemap_segment` führt den Namensraum `xmlns:image` (`landing/views.py:3533`) und `_bild_block` hängt an jeden Eintrag die `<image:image>`-Blöcke seines Pfades (`landing/views.py:3460`) —, und sie zeichnet genau die drei Basis-Pfade aus, die überhaupt ein eigenes inhaltstragendes Bild zeigen: `/` (Hero, Florin, vier Referenzbilder), `/referenzen/` und `/ueber-uns/`; mal drei Sprachfassungen sind das die gemessenen 9 von 165 Einträgen, denn auf den übrigen 162 Adressen steht kein einziges eigenes Bild (`<img>` kommt in `templates/` nur in `index.html`, `referenzen.html`, `ueber_uns.html` und den vier `noindex`-Vorgangsseiten vor, sonst nur Logo und SVG-Symbolsatz). Der Anteil liesse sich also nur heben, indem auf jeder Seite dasselbe Markenzeichen stünde — eine Angabe, die einer Bildersuche nichts sagt, und Bilder für 162 Seiten zu erfinden ist nicht Sache dieses Werkzeugs. | 2026-09-05 |
| GE11 | beim Kunden | `sameAs` steht leer, und das ist kein Versäumnis, sondern die Folge einer fehlenden Zuarbeit: Der Knoten ist gebaut und wartet nur auf Werte — `landing/views.py:1571–1589` hängt `sameAs` an den Betriebsknoten und die LinkedIn-Adressen an den Inhaber, sobald `content.json` → `profile` gefüllt ist; heute steht dort `[]` (`content.json:38`), und der Kommentar darüber (`views.py:1560`) hält seit dem Ausbau 3 fest, warum. `sameAs` ist eine **Identitätsbehauptung**: Ein geratener oder auf gut Glück gesetzter Verweis auf ein fremdes Profil ist schlechter als ein leeres Feld, weil er die Entität mit etwas verknüpft, das nicht der Kunde ist. Was fehlt, sind die Profile selbst — Google-Unternehmensprofil, LinkedIn, WKO Firmen A–Z, Herold.at, Bing Places —, und die legt kein Werkzeug an; sie gehören unter „Beim Kunden" Nr. 1 und in die Zeile „Verzeichniseinträge" unter „Fehlt". | 2026-09-05 |
| PJ08 | bewusst so | Die Dichte lässt sich nur senken, indem man genau die Entscheidungen zurücknimmt, wegen derer diese Seite funktioniert. Nachgezählt am 07.09.2026 über alle `.py`- und Vorlagendateien: **347 der 462 Befunde sind `\|safe`-Ausgaben** — die Sprachpakete sind die vertrauenswürdige Quelle, in `../docs/mehrsprachigkeit.md` begründet und in `10-TECHNIK.md` §Befunde bereits als Freibrief festgehalten; ein entferntes `\|safe` zeigt Markup als Text. **27 sind `print()`-Zeilen** (gezählt: 41 in sechs Dateien, jede mit `[TAG]`-Präfix und `flush=True`) — auf Railway ist das Log die einzige Spur, die einen Deploy überlebt, und seit dem 06.09.2026 hängt daran die Sicherung jeder Anfrage (`views._anfrage_sichern`, `views._send_mail_logged`); sie durch `logging` zu ersetzen wäre ein Umbau am Rettungsweg für einen Zählwert. **19 sind sehr lange Zeilen** — von 2.267 Zeilen über 120 Zeichen liegen 2.218 in `landing/i18n/`, das sind Fliesstexte in Zeichenketten, kein Code; sie umzubrechen änderte den ausgelieferten Text. Von den drei Klassen, die der Rat zum gesammelten Abräumen vorschlägt, gibt es zwei hier gar nicht: Eine Volltextsuche nach `TODO`, `FIXME`, `XXX` und `HACK` über alle Python-Dateien ergibt **null Treffer**, und unbenutzte Einfuhren stehen nicht unter den vier häufigsten Klassen. Bleiben die **32 Module ohne Test** — der einzige echt abräumbare Anteil, und der ist in diesem Paket der eigene Punkt `PJ03` (38 neue Testfunktionen in `landing/tests/test_module.py`, Suite von 214 auf 252). | 2026-09-07 |
| SI12 | nicht anwendbar | Der Punkt misst eine Eigenschaft der Plattform, nicht des Projekts: Das Zertifikat stellt Railway aus und erneuert es selbsttätig; im Repository gibt es weder eine Zertifikatsdatei noch eine TLS-Konfiguration, die Anwendung terminiert kein TLS. `config/settings.py` setzt nur, was hinter dem Proxy gilt (`SECURE_SSL_REDIRECT`, HSTS 31536000 s), und `../docs/DEPLOY.md` §Domain nennt als einzigen Eintrag `https://www.wvm-it.tech`. „Noch 30 Tage gültig" ist bei einem Zertifikat von Let's Encrypt mit 90 Tagen Laufzeit der reguläre Stand unmittelbar vor der automatischen Erneuerung, kein Mangel — es gibt keine Codezeile, mit der sich dieser Wert ändern liesse. Der Teil des Rats, der wirklich offen ist, betrifft die DNS-Einträge der Nebenadressen und steht bereits als eigener Punkt in dieser Tabelle: `TS11`, Apex-Domain auf der Parkseite des Registrars, `verified: false`. | 2026-09-07 |
| MW18 | bewusst so | Der Rat verlangt zwei Dinge, die es hier beide nicht gibt: ein Django-Modell und dessen Eintrag in der Verwaltung. `config/settings.py:150` setzt `DATABASES = {}`, und `INSTALLED_APPS` (`config/settings.py:42–45`) führt genau zwei Einträge — `django.contrib.staticfiles` und `landing`, weder `admin` noch `auth`. Diese Seite benutzt das ORM nicht; ihr einziger Datenbankzugriff ist die gemeinsame Supabase-Warteschlange (`landing/supa.py`), die nicht ihr gehört. **Die Lücke, die der Punkt meint, ist trotzdem geschlossen:** `views._anfrage_sichern()` (`landing/views.py:1131`) schreibt jede Anfrage als eine Zeile JSON auf die Platte **und** ins Log — und zwar **bevor** die erste Mail rausgeht; seit dem 07.09.2026 an allen Anfragewegen statt nur an den Kurzanfragen der Leistungsblöcke. Gespeichert wird, was auch in der Mail steht, ohne IP-Adresse. Dass die Reihenfolge stimmt und die Anfrage einen geworfenen Sendefehler vollständig überlebt, halten drei Testfunktionen in `landing/tests/test_anfrage_sicherung.py` fest. Ein Modell einzuführen hiesse, für einen Zählwert Datenbank, Migrationen und die Django-Verwaltung an eine Seite zu hängen, die ohne alles davon läuft — und die Anfrage wäre danach nicht sicherer aufbewahrt als heute. | 2026-09-08 |
| GE23 | nicht anwendbar | **Auf `main` nicht gebaut.** Der Antwortabsatz für die vier Rechtsseiten liegt fertig auf `sofort/2026-09-08-ge23-und-2-weitere`; das Paket wurde abgewiesen, weil die Designwache den Absatz auch auf `/impressum/` (291 → 292 Elemente) und `/datenschutz/` (301 → 302) fand — zwei Seiten, für die er nicht beauftragt war. Solange darüber nicht entschieden ist, behauptet dieser Eintrag nichts über `main`: `templates/recht.html` bindet dort kein `antwort.html` ein (nachgemessen am 10.09.2026). | 2026-09-10 |
| PJ16 | nicht anwendbar | Der Punkt („nichts Ungepushtes") misst den Abstand des Arbeitszweigs zu `origin` — eine Eigenschaft des Werkzeugablaufs, nicht des Projekts, und die einzige Grösse in diesem Katalog, die **jede** Bausitzung zwangsläufig vergrössert. Grenze 6 jedes Auftrags dieser Seite untersagt `git push` ausdrücklich — `git push`, `gh` und `railway` stehen in keinem Werkzeugsatz der Sitzung —, zugleich verlangt derselbe Auftrag einen Commit. Beide Regeln zusammen ergeben nach jeder Arbeit genau den Zustand, den `PJ16` als Mangel liest: sauberes Arbeitsverzeichnis, aber Commits vor `origin`. Nachgemessen am 08.09.2026 unmittelbar vor dem Nachbesserungs-Commit: `git status --short` nur die Zeile dieser Datei, `git log --oneline origin/main..HEAD` = die drei Commits des Pakets, `origin/main` unverändert bei `7f784a8`. Es gibt keine Codezeile, mit der sich der Wert innerhalb der Grenzen ändern liesse — er fällt in dem Moment auf null, in dem das Werkzeug nach dem Doku-Durchgang pusht. Deshalb ist der Punkt hier nicht messbar und wird nicht als erledigt behauptet. | 2026-09-08 |
| GE24 | bewusst so | Der Rat des Katalogs — vorhandene Zwischenüberschriften in die Frage umschreiben, die ein Mensch tippen würde — verlangt genau das, was Grenze 1 jedes Auftrags dieser Seite untersagt: Überschriftentexte bleiben unverändert, weil die Designwache sie vergleicht. Gebaut ist die Sache ohnehin: Eine Messung aller 165 Adressen am 05.09.2026 zählt **87 mit mindestens einer Frage-Zwischenüberschrift** (72 davon auf Ebene `h2`) und **27, die die Frage schon als `h1` stellen** — die FAQ-Blöcke der Leistungs-, Branchen- und Vergleichsseiten wickeln jede Frage in `<h3 class="faq-q-h">` (`templates/leistung.html:90`, `branche.html:96`, `vergleich.html:106`). Die sieben Regionsseiten stellen ihre je zwei bis drei FAQ-Fragen in drei Sprachen als `<summary>`-Text (`templates/region.html:84`); als `Question`-Knoten stehen sie trotzdem im Graphen (`views.py:2688`), sind für eine Antwortmaschine also nicht verloren. Was bliebe, wären die Abschnittsüberschriften der lehrenden Silos — im Glossar die feste Folge Definition, Erklärung, Praxisbezug, Irrtum (`templates/begriff.html`), die als Aussage richtig ist und als Frage nur verkleidet wäre. | 2026-09-05 |
| PF18 | bewusst so | Auf **161 der 170** gemessenen Seiten ist das erste Bild im `main` dasselbe: das 44 px breite, `aria-hidden` gesetzte Porträt **unten** in der Anfragekarte (`templates/anfrage_karte.html:45–47`, `sizes="44px"`, `loading="lazy"`) — erstes Bild nur deshalb, weil oberhalb überhaupt keines steht. Der Beleg der Messung sagt genau das: Er nennt für `/kontakt/`, `/kosten/rechner/` und die übrigen Formularseiten `florin_320.webp`, also dieses Dekobild. Ihm hohe Ladepriorität zu geben zöge es vor den sichtbaren Inhalt und verschlechterte die Messung, die der Punkt verbessern soll — als LCP-Element kommt ein 44-px-Bild am Fuss eines Formulars nie in Frage. **Wo der Punkt zutrifft, ist er gebaut:** das Hero-Porträt (`templates/index.html:69`), das Porträt auf `/ueber-uns/` (`templates/ueber_uns.html:48`) und das erste Referenzbild (`templates/referenzen.html:43`) tragen `fetchpriority="high"` — mal drei Sprachfassungen sind das genau die 9 von 170 Seiten, die die Messung als bestanden zählt. Beide Seiten der Entscheidung hält `landing/tests/test_bilder.py` fest, einschliesslich der Prüfung, dass kein Bild `fetchpriority="high"` und `loading="lazy"` zugleich trägt. Die Begründung steht seit dem 07.09.2026 als Kommentar in `anfrage_karte.html:39–44`, damit der nächste Durchgang den Befund nicht wörtlich abarbeitet. | 2026-09-10 |
| IS18 | bewusst so | Der Katalog misst gegen einen Sollwert je Seitenart, und die Seitenarten dieser Website kommen darin nicht vor. **Vier der fünf namentlich genannten Seiten liegen bei 83 bis 98 Prozent** des Solls — `/vergleich/it-betreuung-vs-stundenabrechnung/` mit 745 von 900, `/vergleich/` mit 843, `/ro/vergleich/` mit 874, `/en/vergleich/` mit 882. Auf der Vergleichsseite stehen eine siebenzeilige Gegenüberstellung, je vier Fälle für **beide** Richtungen, ein Rechenweg in zwei Schritten und vier Fragen mit Antwort (`landing/i18n/vergleiche_de.py:19–66`); es fehlt dort nichts, was ein Leser vermisst — die letzten 155 Wörter wären Füllung, und Füllung ist auf dieser Seite als Fehler festgelegt (Relaunch-Entscheidung E7, „durch Verzicht, nicht durch Dekoration", siehe [20-DESIGN.md](20-DESIGN.md)). Die fünfte genannte Seite ist ein **Hub**: `/branchen/` trägt 648 Wörter im `main` (nachgezählt am 10.09.2026 über alle 94 Basis-Pfade), von denen das Werkzeug 301 als eigene wertet — der Rest sind die Kacheltexte und die Zusammenfassung dessen, was auf den sechs verlinkten Seiten ausführlich steht. Genau das ist die Aufgabe eines Wegweisers; mehr Eigentext dort hiesse entweder die Leistungsseiten zu wiederholen oder Unterschiede zwischen Branchen zu behaupten, die es technisch nicht gibt — der Hub sagt in `ehrlich_t` ausdrücklich das Gegenteil. Die eigene Umfangsprüfung `python manage.py seo_bericht` meldet über alle 94 Basis-Pfade bei 176.797 Wörtern und einem Schnitt von 822 je Seite **genau eine** auffällige Adresse: `/impressum/` mit 152 Wörtern (Befehl am 10.09.2026 zuletzt nach den SEO-Änderungen aus der Search Console nachgefahren; nach `GE26` 176.675, davor 175.493 Wörter und Schnitt 818) — ein Rechtstext, bei dem die Vollständigkeit nach § 5 ECG zählt und nicht der Umfang, und was dort fehlt, sind zwei Pflichtangaben, die nur Florin liefern kann (siehe „Beim Kunden"). Wo Umfang wirklich fehlte, ist er in diesem Paket gewachsen: `GE26` hat vier Seiten dreisprachig um einen Definitionsabsatz ergänzt. | 2026-09-10 |
| PF17 | bewusst so | Der Punkt ist die Kehrseite von `PF18` und trifft **dasselbe eine Bild**: Nachgemessen am 10.09.2026 über alle 94 Basis-Pfade ist auf **77** davon das erste Bild im `main` das 44 px breite, `aria-hidden` gesetzte Porträt **unten** in der Anfragekarte (`templates/anfrage_karte.html:45–47`, `sizes="44px"`) — erstes Bild nur deshalb, weil oberhalb überhaupt keines steht; mal drei Sprachfassungen sind das die 161 gemeldeten Adressen, und der Beleg der Messung nennt für jede davon `florin_320.webp`. Es eifrig zu laden hiesse, ein Dekobild am Fuss eines Formulars vor den sichtbaren Inhalt zu ziehen — genau die Verschlechterung, die `PF18` am 07.09.2026 für die hohe Ladepriorität schon abgewehrt hat. **Die zweite Hälfte des Rats ist erfüllt:** von den 8 Bildern, die im `main` überhaupt nach dem ersten stehen, sind 8 verzögert; die drei eifrig geladenen Bilder der ganzen Seite sind das Hero-Porträt, das Porträt auf `/ueber-uns/` und das erste Referenzbild — also je das echte LCP-Element, gesichert von `landing/tests/test_bilder.py::LadeprioritaetTest`. Was die Messung ausserdem als „nicht verzögert" zählt, ist auf jeder Seite das Markenzeichen im Fuss (`templates/base.html:170`): dieselbe Adresse wie im Kopf (`base.html:95`), 30 px, also derselbe Abruf aus dem Zwischenspeicher — ein `loading="lazy"` daran spart keinen einzigen Abruf und hebt nur den Zählwert. | 2026-09-10 |
| PJ07 | bewusst so | Der Anteil steigt nur, wenn eine Datei **alle** ihre Befunde verliert — und die 85 Dateien mit Befund tragen fast durchweg solche, die diese Seite bewusst trägt. Nachgezählt am 10.09.2026: **42 der 46 Vorlagen** enthalten `\|safe`, zusammen **1.320 Vorkommen**; ohne sind nur `honigtopf.html`, `icons.html`, `icons_sprite.html` und `lang_switch.html`. Die Datei-Prüfung meldet **je Zeile** einen Befund (gedeckelt bei zehn je Datei) — `templates/404.html` hat 24 solcher Zeilen, `templates/newsletter_confirm.html` 77. Sauber würde eine Vorlage also erst, wenn **jede** dieser Zeilen einen Freibrief bekäme; das Werkzeug selbst nennt das Streuen und weist Freibriefe getrennt aus. Und ein entferntes `\|safe` zeigt Markup als Text: Die Sprachpakete sind die vertrauenswürdige Quelle (`../docs/mehrsprachigkeit.md`), festgehalten in [10-TECHNIK.md](10-TECHNIK.md) §Befunde. Die zweitgrösste Gruppe waren **34 Python-Module ohne Test**; sie ist mit `PJ03` in diesem Paket abgeräumt, und die Dateien, die ausser diesem einen Befund nichts trugen, werden damit sauber — `branchen.py`, `checklisten.py`, `context.py`, `glossar.py`, `regionen.py`, `vergleiche.py`, `selbsttest.py`, `stand.py` und `indexnow.py` haben weder eine Zeile über 120 Zeichen noch ein `print(`. Was danach bleibt, ist zweimal dieselbe schon unter `PJ08` begründete Entscheidung: die 24 `print(`-Zeilen in `landing/views.py` (auf Railway die einzige Spur, die einen Deploy überlebt, und seit dem 06.09.2026 hängt daran die Sicherung jeder Anfrage) und die langen Zeilen der Sprachpakete, die Fliesstext sind und kein Code. Eine eigene Änderung für diesen Punkt gäbe es nur um den Preis genau dieser Entscheidungen. | 2026-09-10 |
| GE26 | nicht anwendbar | Die Regel erkennt einen Definitionssatz nur an **deutschen** Wendungen („ist ein“, „bedeutet“, „kostet“, „beginnt bei“ … — Prüfwerkzeug `cockpit/pruefer/katalog/r_geo.py:65–68`), und was offen gemeldet wird, sind die englischen und rumänischen Fassungen: Die zuerst genannten tragen ihren Definitionssatz in ihrer Sprache und zählen trotzdem als „ohne“ — `/en/kontakt/` „remote support means that a technician …“ (`landing/i18n/en.py:942`), `/ro/kontakt/` „asistența la distanță înseamnă că …“ (`ro.py:938`), `/en/angebot/` „A guide price means …“ (`en.py:1024`), `/ro/angebot/` „Preț orientativ înseamnă …“ (`ro.py:1020`), `/en/leistungen/` „Running support starts at €29 per workstation per month“ (`en.py:855`); eingebaut mit `faf3716` und `8f75efb`, seit dem 10.09.2026 auf `main`. Die deutschen Fassungen derselben Seiten gelten als erfüllt — in der Messung vom 11.09.2026 (112 von 198) stehen `/kontakt/`, `/angebot/` und `/leistungen/` nicht mehr in der Liste, der Beleg dieses Auftrags (110 von 197, noch mit `/kontakt/`) stammt aus einem Lauf davor —, und die 86 gemeldeten Adressen stehen 104 englischen und rumänischen gegenüber (52 dreisprachige Basis-Pfade laut `python manage.py seo_bericht --inventar`). Ein deutsches Signalwort in einen englischen oder rumänischen Text zu setzen, damit das Muster greift, hiesse die Übersetzung zu verfälschen. | 2026-09-11 |
| PF16 | bewusst so | Jedes Inhaltsbild trägt bereits `srcset` und `sizes` — Hero-Porträt, zweites Porträt der Startseite (`.vt-photo`), Roboter, Referenz- und Kooperationsbilder (`templates/index.html:66–69`, `:327–330`, `:785–788`, `:809–848`, `:867–870`), das Porträt der Anfragekarte (`anfrage_karte.html:45–47`), die Referenzen (`referenzen.html:40–42`) und `/ueber-uns/` (`ueber_uns.html:44–46`) —, und die Lücke der Messung ist **genau ein Bild**: 590 − 194 = 396 = 198 Seiten × 2 (am 11.09.2026 ebenso 600 − 202 = 398 = 199 × 2), nämlich das Markenzeichen `wvm_mark.webp` im Kopf und im Fuss (`templates/base.html:95` und `:170`, gleichlautend `angebot.html:56` und `:305`). Das ist kein Inhaltsbild, sondern Schmuck (`alt=""`, `aria-hidden="true"`), mit 30 px unter der Grenze von 32 px, ab der diese Seite ein `srcset` verlangt (`CLAUDE.md` Zeile „Bildgrößen“, `landing/tests/test_bilder.py:30` `EGAL_BIS = 32`), und die Datei wiegt als 128-px-WebP **2.754 Byte** — nahe an der Fassung, die ein Bildschirm mit dreifacher Pixeldichte für 30 px ohnehin braucht (90 px). Kopf und Fuss teilen sich dieselbe Adresse, also ein Abruf je Seite; zwei kleinere Zusatzfassungen könnten nur auf Bildschirmen mit einfacher oder doppelter Dichte einen Teil dieser 2,7 KB sparen und brächten zwei weitere Dateien in die Pflege, die bei jeder Änderung des Zeichens mitgezogen werden müssten. | 2026-09-11 |
| PJ06 | bewusst so | Die Note ist der Mittelwert der Dateinoten, jede Dateinote der Anteil ihrer bestandenen Einzelprüfungen, und ein Freibrief nimmt nur den Befund aus der Liste, nicht den Fehlschlag aus der Note (Prüfwerkzeug `cockpit/pruefer/codeaudit.py:1412–1430` und `:1449`). Nachgerechnet über die **137 Dateien mit Note** (76 Python, 46 Vorlagen, 6 Skripte, 2 Stilvorlagen, 7 Konfigurationsdateien; die 44 Markdown- und Shell-Dateien tragen keine): Die Prüfung `V02` („Ausgabe ohne Maskierung“) schlägt in **42 der 46 Vorlagen** an (1.320 `\|safe`), bei 12 Prüfungen je Vorlage sind das 42 ÷ 12 = **3,5 Dateien** Abzug — die Note kann damit selbst bei sonst fehlerfreiem Code nicht über 1 − 3,5 ÷ 137 = 0,974 steigen, ohne `V02` stünde sie heute bei rund 0,99. Für gerundet 0,970 bleiben allen übrigen Befunden zusammen **0,68 Dateien** Spielraum, und schon zwei weitere begründete Entscheidungen kosten mehr: die Fliesstextzeilen über 140 Zeichen in den 18 Sprachpaketen (`P17`, 18 ÷ 32 = 0,56) und die `print(…, flush=True)`-Zeilen in sechs Dateien (`P03`, 6 ÷ 32 = 0,19), auf Railway die einzige Spur, die einen Deploy überlebt. Erreichbar wäre die Schwelle also nur, indem `\|safe` aus den Vorlagen fällt, obwohl die Sprachpakete Markup tragen (etwa `</p><p class='sp-intro'>` in `kontakt_seite.intro`, `landing/i18n/de.py:968`), oder die Sprachtexte zerlegt und das Log umgebaut werden — genau die Entscheidungen, die unter `PJ07` und `PJ08` schon begründet stehen. | 2026-09-11 |
| KV04 | bewusst so | Der Rat ist erfüllt: Die Formulare fragen schon heute nur Rückkanal, Name und Nachricht ab. Die Messung zählt aber jedes `input`, `select` und `textarea`, auch die unsichtbaren (Prüfwerkzeug `cockpit/pruefer/crawl.py:595` und `:600`). Die Kurzanfrage der Unterseiten (`templates/anfrage_karte.html`) kommt so auf 6 Felder. **Vier davon sieht kein Mensch:** `csrfmiddlewaretoken`, `quelle`, `zurueck` und der Honigtopf `website` (`anfrage_karte.html:16–19`). Sichtbar sind 2, und nur der Rückkanal ist Pflicht (`:64`). Die 4 technischen Felder muss die Seite haben: CSRF-Schutz und Honigtopf verlangt `pruefe_seite`, `zurueck` bringt Besucher ohne JavaScript auf ihre Seite zurück. Über 6 liegen nur **15 der 398 Formulare**, also 5 Formulare mal 3 Sprachen, und keines davon lässt sich kürzen, ohne Wesentliches wegzunehmen. **Rückruf im Hero, 7 Felder** (`templates/index.html:138–165`): 4 technische, dazu Name (freiwillig), Telefon (Pflicht) und ein vorausgewähltes Zeitfenster. **Kontaktformular, 8 Felder** (`index.html:1150–1174`): Honigtopf und CSRF, dazu Name, E-Mail und Nachricht als Pflicht, Telefon und Budget mit dem Zusatz „optional“, dazu die Einwilligung. **Selbsttest, 7 Felder** (`templates/selbsttest.html:140` mit `werbung=1`): die Kurzanfrage plus das Kästchen für die Werbeeinwilligung. Es muss nach § 174 TKG 2021 und Art. 7 DSGVO getrennt und nicht vorausgewählt dastehen (`anfrage_karte.html:67–83`). **Richtangebot, 37 Felder** (`index.html:486–579`), und **`/angebot/`, 43 Felder** (`templates/angebot.html:120–255`): Das sind die Konfiguratoren. Fast alle Felder sind Auswahlkästchen für die 33 Positionen aus `ANGEBOT_GROUPS`, keine Fragen. Beim Richtangebot ist nur die E-Mail Pflicht. `/angebot/` hat zusätzlich 3 Mengenfelder für die Positionen mit `menge_max` (`landing/views.py:159`, `:162`, `:164`). Ohne sie wurde bis zum 06.09.2026 aus einem Angebot über 370 € eines über 167 € (`../CLAUDE.md`, Zeile „Mengen“). Wer weniger ausfüllen will, findet auf jeder Seite den Rückruf-Dialog mit einem einzigen Pflichtfeld (`templates/base.html:130–157`). Kürzer ginge es nur, wenn Felder, Formulare oder die getrennte Einwilligung wegfallen. Das verbietet Grenze 1 des Auftrags, und bei der Einwilligung das Gesetz. | 2026-09-11 |
| VL01 | bewusst so | Der Teil der Regel, der ihren Zweck trägt, ist erfüllt: `manage.py`, `config/settings.py`, `config/urls.py` und `config/wsgi.py` liegen an der vereinbarten Stelle. Die zwei fehlenden Merkmale verlangen einen Umzug, der diese Seite etwas kostet, das sie sich eigens gebaut hat. **Erstens `apps/`:** `stand_schreiben` datiert jede der 198 Adressen über `git log -1 --format=%cs -- <datei>` **ohne `--follow`** (`landing/management/commands/stand_schreiben.py:155–157`), und zwar auf Pfade wie `landing/leistungen.py` und `landing/i18n/seiten_de.py` (`:40–79`). Nach einem Umzug nach `apps/landing/` hätte jede Quelldatei den Umzugs-Commit als letzten Stand. Alle Seiten trügen dann in Sitemap (`lastmod`) und Schema (`dateModified`) dasselbe Datum, also genau der Zustand, gegen den `landing/stand.py` gebaut wurde (`stand_schreiben.py:31–36`, Befunde `TS16`/`GE18`). Dazu kämen 110 Importe in 21 Python-Dateien und 134 Pfadangaben in 25 Doku-Dateien, und das für eine einzige App: `INSTALLED_APPS` führt neben `django.contrib.staticfiles` nur `landing` (`config/settings.py:42–45`), ein `apps/`-Ordner hätte also nichts zu ordnen. **Zweitens `data/`:** Die Inhaltskonstanten stehen schon heute in reinen Datenmodulen ohne Django-Import, sogar ganz ohne Import: `landing/leistungen.py`, `branchen.py`, `vergleiche.py`, `regionen.py`, `beitraege.py`, `glossar.py`, `checklisten.py`, `einrichtungen.py`, `selbsttest.py` und `stand.py` enthalten keine einzige `import`-Zeile (nachgezählt am 11.09.2026). Die Messung zählt dagegen nur Python-Dateien, in deren Pfad ein Ordner namens `data` vorkommt, und prüft deren Importe nicht (Prüfwerkzeug `cockpit/pruefer/statik.py:158`). Ein Ordner nur für diesen Zähler würde die Seite nicht besser machen, wohl aber das Datum jeder Silo-Seite zurücksetzen (`stand_schreiben.py:64–79`). | 2026-09-11 |
| IS06 | bewusst so | 48 der 58 gemeldeten Titel nennen Ort oder Nutzen schon, die übrigen 10 lassen beides aus gutem Grund weg. Die Regel erkennt aber als Ort nur **Lenzing**, den Teil vor dem Komma im Ort des Betriebs (Prüfwerkzeug `cockpit/pruefer/katalog/r_seo_inhalt.py:105–107`), und als Nutzen nur 26 feste Wörter (`:60–63`). Nachgemessen am 11.09.2026 über alle 198 Adressen, dieselben 58 wie im Beleg: **33 nennen einen Ort**, nämlich die 21 Regionsseiten ihre Stadt („IT-Service Linz — EDV-Betreuung für Betriebe“, `landing/i18n/regionen_de.py:200`, ebenso Vöcklabruck, Attersee, Gmunden, Bad Ischl, Wels und Salzburg), der Regionen-Hub „Oberösterreich und Salzburg“ (`de.py:507`) und `/leistungen/`, Smarthome und Veranstaltungstechnik „Österreich“ bzw. „Oberösterreich“ (`de.py:850`, `seiten_de.py:429`, `:519`), jeweils in drei Sprachen. „Lenzing“ in den Titel der Linzer oder Salzburger Seite zu setzen, verwässerte genau die Seite, die für diese eine Stadt gebaut ist. **15 Fachbeiträge und Glossareinträge** sagen in eigenen Worten, was der Leser davon hat: „ohne Datenverlust“, „keine Aussetzer mehr“, „Hilfe in Minuten, ohne Anfahrt“ (`beitraege_de.py:279`, `:123`, `glossar_de.py:22`). Drei davon tragen eine ausgeschriebene Zahl („die vier häufigsten Ursachen“, „die drei Fragen, die zählen“, „zwei Zeiten, ein Unterschied“, `beitraege_de.py:626`, `glossar_de.py:112`, `:202`), die die Regel nur als Ziffer zählt (`r_seo_inhalt.py:244`). **6 Einrichtungsseiten** (Server, Loxone, je drei Sprachen) dürfen keine Zahl tragen, weil sie keinen Festpreis haben und das unter eigener Überschrift begründen (`landing/i18n/einrichten_de.py:402`, `:657`; `CLAUDE.md`, Zeile „Anfrage-Preise“). Ein Preis im Titel wäre dort genau die Angabe, die eine Antwortmaschine als verbindlich liest. Der Firmensitz gehört dort ebenso wenig hinein, weil keine der beiden Leistungen an ihm hängt: Die Bestandsaufnahme für den Server läuft auch per Fernwartung (`einrichten_de.py:408`), und Loxone-Anlagen bietet die Leistungsseite für ganz Österreich an (`seiten_de.py:429`). **4 Rechtsseiten** (`de.py:1048–1054`) sind Zusagen, keine Werbeflächen: Ein Versprechen in ihrem Titel wäre eine Zusage, die der Text darunter nicht gibt. Der Ort steht im Impressum selbst, wo die Anbieterkennzeichnung ihn verlangt, und im Titel fände ihn nur, wer ohnehin nach WVM-IT sucht. Erreichen liesse sich die Wertung also nur mit „Lenzing“ auf Seiten, die für andere Orte oder für ganz Österreich gebaut sind, mit Wörtern aus der Liste statt der gewählten oder mit Zahlen, die die Seite ausdrücklich nicht nennt. | 2026-09-11 |
| PJ11 | nicht anwendbar | Das Lockfile liegt daneben, die Messung kann es nur nicht lesen. `requirements.lock` hält seit dem 05.09.2026 alle zwölf Pakete mit `==` fest, die fünf direkten und die mittelbaren (`requirements.lock:31–42`); `requirements.txt` nennt die fünf direkten mit `==` (`:18–22`) und bindet das Lockfile seit dem 11.09.2026 per `--constraint requirements.lock` in jeden Deploy ein (`:23`), der CI-Lauf installiert direkt daraus (`.github/workflows/pruefen.yml:63`). Die Regel sucht zwar genau nach diesem Namen (Prüfwerkzeug `cockpit/pruefer/katalog/r_projekt.py:365`), bekommt ihre Dateiliste aber aus dem Code-Audit, und das nimmt nur Dateien mit bekannter Endung oder bekanntem Namen auf (`cockpit/pruefer/codeaudit.py:47–63`, übersprungen in `:1280–1281`). `.lock` steht in keiner der beiden Listen. Damit ist jeder der vier Namen, die die Regel als Lockfile gelten lässt (`requirements.lock`, `uv.lock`, `poetry.lock`, `pdm.lock`), für sie unsichtbar, und die 80 Prozent lassen sich aus keinem Projekt heraus heben. Eine Umbenennung, etwa in `requirements-lock.txt`, träfe die Namensliste der Regel ebenso wenig und müsste `requirements.txt:23`, den CI-Lauf und beide Kopfkommentare mitziehen, ohne dass sich die Messung bewegt. Derselbe Befund ist deshalb schon zweimal als erledigt gemeldet worden (`../docs/BEFUNDE-281-2026-09-06.md:27`, Commit `d87313f`) und kam beide Male wieder. | 2026-09-11 |
| SU08 | bewusst so | Keiner der vier gemeldeten Pfade ist ein Ordner mit einer einzelnen Datei darin. Jeder ist eine Einzelseite, die ihr Thema auf einer Adresse vollständig abdeckt. **`/barrierefreiheit/`** ist die Barrierefreiheitserklärung, ein Pflicht-Seitentyp wie Impressum, Datenschutz und AGB (`config/urls.py:115–119`). Diese drei nimmt die Regel aus der Silo-Zählung heraus (Prüfwerkzeug `cockpit/pruefer/katalog/r_substanz.py:27`), die Erklärung nicht, obwohl das Werkzeug sie an anderer Stelle als Rechtsseite erkennt (`r_recht.py:525`). **`/it-notfall/`** hat seinen kurzen Pfad mit Absicht: „Diese Adresse wird im Ernstfall abgetippt“ (`config/urls.py:75`). Unter einem Oberordner wäre sie länger und schwerer zu merken, und die Seite deckt mit den ersten dreissig Minuten und vier Fällen das ganze Thema ab (`landing/views.py:2772`). **`/it-sicherheit-test/`** ist ein Werkzeug: zehn Fragen, sofortiges Ergebnis, nichts gespeichert (`landing/views.py:2690`). Aus der Auswertung verweist es auf die passende Leistungsseite (`templates/selbsttest.html:107–109`), und dort ist das Thema ausgebaut. **`/referenzen/`** ist die Übersicht, die alle Referenzen in einer Schleife auf einer Seite zeigt (`templates/referenzen.html:31–59`). `REFERENZEN` führt nur Projekte, deren Kunden der Nennung zugestimmt haben, heute eines (`landing/views.py:3185–3198`). Weitere Fallstudien (unter „Fehlt“) kämen auf dieselbe Seite und ergäben keinen zweiten Pfad. Die Zählung liesse sich nur auf zwei Wegen heben, und beide schaden der Seite. Entweder man verschiebt die Adressen unter einen gemeinsamen Ordner: Das hiesse, neun indexierte Adressen (drei Seiten, je drei Sprachen) umzuleiten, darunter die Notfalladresse, die gerade kurz sein soll. Oder man legt Unterseiten an: Das wären Füllseiten ohne eigenen Inhalt oder Referenzen ohne Zustimmung der Kunden. Die beiden Werkzeugseiten stehen schon in dieser Datei als „bewusst einzeln“ (Zeile „12. Themenbereiche und Ortsseiten“). | 2026-09-11 |
| VL21 | bewusst so | Von den drei Baustein-Merkmalen tragen zwei den Zweck der Regel, und die sind erfüllt; offen ist allein die Ordnerkonvention. Der Angebotsblock steht genau **einmal** — `templates/anfrage_karte.html` — und wird von dort in **13 Vorlagen** eingebunden (`leistung.html:156`, `branche.html:107`, `vergleich.html:131`, `region.html:106`, `einrichtung.html:143`, `beitrag.html:125`, `begriff.html:71`, `checkliste.html:105`, `kontakt.html:56`, `rechner.html:170`, `notfall.html:120`, `selbsttest.html:140`, `ueber_uns.html:99`). Eine zweite Fassung desselben Blocks gibt es im Projekt nicht, und seine beiden Pflichtteile sind ihrerseits Bausteine, deren Einbindung `pruefe_seite` erzwingt (`honigtopf.html`, `datenschutzhinweis.html`). Die zwei weiteren Dateien im Beleg sind keine Bausteine, sondern Ansichten: `landing/views.py:1465` rendert `anfrage_done.html`, `:3414` rendert `angebot.html` — in einen Teil-Ordner gehören sie nicht. Den Baustein nach `templates/teile/` zu verschieben hiesse, die 13 einbindenden Vorlagen anzufassen, und **über genau diese Dateien datiert `stand_schreiben` die Seiten** (`landing/management/commands/stand_schreiben.py:41–79`, `git log -1 --format=%cs` ohne `--follow`, `:155–157`). Betroffen wären 70 Basis-Pfade und damit 140 der 198 Adressen: 13 Leistungs-, 6 Branchen-, 4 Vergleichs-, 7 Regions-, 18 Beitrags-, 3 Checklisten- und 14 Glossarseiten samt Kontakt, Rechner, Notfall, Selbsttest und Über uns. Sie alle trügen danach in Sitemap (`lastmod`) und Schema (`dateModified`) den 12.09.2026, ohne dass sich ein einziger Inhalt geändert hätte — der Kopf des Befehls hält ausdrücklich fest, dass eine bauliche Änderung kein Änderungsdatum ist (`:31–36`), und die Falle steht als `TS16`/`GE18` in [10-TECHNIK.md](10-TECHNIK.md). Dieselbe Rechnung hat am 11.09.2026 bereits `VL01` entschieden. Der Ausweg, den bisherigen Aufrufnamen als reine Weiterleitung stehen zu lassen, brächte eine Datei ohne eigenen Zweck und liesse den Block danach in zwei Dateien stehen statt in einer — mehr Streuung, nicht weniger. | 2026-09-12 |
| VL13 | bewusst so | Die drei Formulare ohne POST sind **Abfragen, kein Datenversand**; bei ihnen ist GET nicht der Mangel, sondern die richtige Methode. Es sind genau drei Basis-Pfade in je drei Sprachen, also die neun gemeldeten Adressen: der Kurzrechner der Startseite (`templates/index.html:223`, führt auf `/kosten/rechner/`), der Kostenrechner selbst (`templates/rechner.html:44`) und der Sicherheits-Selbsttest (`templates/selbsttest.html:57`). Keines schreibt etwas — beide Ansichten lesen ausschliesslich `request.GET` (`landing/views.py:706` und `:2704`). Dass das eine Entscheidung ist und kein Versehen, steht seit dem Bau im Code: „Ein GET-Formular, kein POST: Die Eingaben stehen in der Adresse, das Ergebnis lässt sich verschicken, und die Seite funktioniert ohne JavaScript vollständig“ (`views.py:698–701`), und beim Selbsttest „Die Antworten kommen als GET-Parameter und werden **nicht** protokolliert … deshalb entsteht auch keine neue Datenverarbeitung, die in `content.json` beschrieben werden müsste“ (`views.py:2691–2695`). Auf POST umgestellt verlöre der Rechner genau das, wofür er gebaut ist: Das Ergebnis wäre nicht mehr verlinkbar, nicht als Lesezeichen zu sichern und nicht weiterzuschicken, und jeder Sprung zurück quittierte mit „Formular erneut senden“. Beim Selbsttest käme hinzu, dass zehn Antworten zur Sicherheitslage eines Betriebs dann im Anfragekörper stünden statt in einer Adresse, die der Besucher selbst in der Hand hat. **Die drei übrigen Bauvorschriften erfüllen auch diese Formulare:** jedes Feld hat ein verknüpftes Label (`rechner.html:49`, `:56`; `index.html:226`, `:230`) oder steht in `fieldset` mit `legend` (`selbsttest.html:61–62`), und jedes hat einen sichtbaren Absendeknopf (`rechner.html:94`, `index.html:235`, `selbsttest.html:74`). Ein CSRF-Token gehört nicht dazu: Er schützt zustandsändernde Anfragen, und diese drei ändern nichts. Die beiden übrigen GET-Formulare der Seite sind Suchfelder mit `role="search"` (`suche.html:28`, `404.html:27`) auf Seiten, die `noindex` tragen und nicht zu den 198 zählen. | 2026-09-12 |
| PF13 | bewusst so | Das weit gesetzte Ablaufdatum steht längst — der Beleg der Messung nennt es selbst: `cache-control: max-age=31536000, public` an beiden geprüften Dateien, also das Jahr, das der Rat verlangt (`config/settings.py:177`, `WHITENOISE_MAX_AGE`). Gemeldet wird allein das fehlende `immutable`, und genau das schliesst der Rat in seinem zweiten Halbsatz selbst aus: „ohne Hash im Namen darf das nicht sein". **Die Namen tragen hier keinen Hash.** `STORAGES` setzt `whitenoise.storage.CompressedStaticFilesStorage`, nicht die Manifest-Fassung (`config/settings.py:171–173`) — bewusst, damit die unter `/static/` vergebenen Pfade auch für dynamisch eingebaute Lead-Fotos stabil bleiben. Die Frische von CSS und JavaScript hängt stattdessen an der Versions-Abfrage `?v={{ asset_v }}`, die jede Vorlage anhängt (`templates/base.html:62–63` und `:251–252`) und die auf Railway der Git-Commit-SHA füllt (`config/settings.py:205–206`); genau diese beiden Dateien nennt der Beleg, `fonts.css?v=9ae39aec8a33` und `main.js?v=9ae39aec8a33`. `immutable` sagt einem Browser: frag nie wieder nach, auch nicht bedingt. Unter der Adresse **ohne** Abfrage — und WhiteNoise liefert dieselbe Datei auch so aus — sässe damit jeder Zwischenspeicher, der die Datei einmal ohne `?v=` geholt hat, ein Jahr auf einem überholten Stand, ohne Weg zurück. Wo es gefahrlos geht, ist es gebaut: `_immutable_file_test` vergibt `immutable` an Schriften, Bilder und Videos, weil die im Bestand nicht überschrieben, sondern unter neuem Namen ersetzt werden (`config/settings.py:180–203`, Begründung im Kopf der Funktion). Den Zählwert heben liesse sich nur, indem CSS und JavaScript ein Versprechen tragen, das die Auslieferung nicht halten kann, oder indem die Seite auf Manifest-Storage umgestellt wird — dann aber verlieren die dynamisch eingebauten Fotos ihre festen Pfade, für einen Zuwachs von rund 0,05 Punkten. | 2026-09-12 |
| PF22 | beim Kunden | Der Befund stimmt (188 KB `style.css`, 204 Kommentarblöcke, die Lighthouse als ungenutzt zählt), aber den Einbau (Kommentare beim `collectstatic` entfernen, Quelldatei unverändert) muss der Betreiber in einem Lauf vergeben, der Python und `git` ausführen darf, denn diese Sitzung konnte weder `manage.py check` noch die Tests noch `git commit` starten. | 2026-09-11 |
| SU05 | bewusst so | Die achte Ortsseite ist in diesem Projekt keine offene Aufgabe, sondern eine **getroffene und belegte Entscheidung dagegen**: `docs/SEO-AUSBAU-3.md` (§ „Nicht enthalten", Zeile 286) führt „Weitere Ortsseiten" unter den verworfenen Ideen mit der Begründung „Sieben genügen für das echte Einzugsgebiet. Mehr wären Doorway-Pages (A16)", und Regel A16 in `docs/SEO-PLAN.md` steht nicht auf einer Meinung, sondern auf einer Messung: Bei einer Schwesterseite standen 131 fast identische Stadtseiten online, **88 % textgleich, Position 85–90, „Gefunden – zurzeit nicht indexiert"**, danach per 301/410 entsorgt. `landing/regionen.py` schreibt die zwei Bedingungen fest, unter denen es die sieben überhaupt gibt — nur Orte in rund einer Fahrstunde um Lenzing (Zeile 20–22) und **keine erfundenen Referenzen** (Zeile 30–31) —, und damit schliesst der Rat des Katalogs die achte Seite selbst aus: Er verlangt „einen echten Auftrag von dort", und genau den gibt es für keinen achten Ort. Den Rat im Übrigen erfüllen die sieben vorhandenen bereits (Vöcklabruck 6 km, Attersee-Region 8, Gmunden 22, Bad Ischl 38, Wels 40, Salzburg 55, Linz 60 — je mit nachgemessener Entfernung, Fahrzeit und eigenem Ortsbezug, 542–619 Wörter, `doku/50-LOCAL-SEO.md`); die Zielgrösse 8 ist eine Zahl des Katalogs, das tatsächliche Einzugsgebiet ist die Bedingung dieser Seite. Belegt ein Auftrag später einen achten Ort, entsteht die Seite — auf Vorrat entsteht sie nicht. | 2026-09-12 |
| SI03 | beim Kunden | Von den zwei Angaben, die der Punkt verlangt, steht eine seit dem 05.09.2026: `SECURE_HSTS_INCLUDE_SUBDOMAINS` ist auf `True` vorbelegt (`config/settings.py:261`), und der Kommentar darüber nennt den Grund — es gibt keine Subdomain, die absichtlich ohne HTTPS bedient wird, `wvm-it-shop.up.railway.app` leitet seit dem 28.08.2026 per 301 auf die Hauptdomain um und spricht dabei HTTPS. Offen ist allein **`preload`**, und das steht nicht aus Versehen auf `False`, sondern mit Begründung im Code (`config/settings.py:262–266`): Die Apex-Domain `wvm-it.tech` parkt beim Registrar (A-Record `213.145.224.30`, Railway meldet `verified: false` — „Beim Kunden" Nr. 3, derselbe Grund wie bei `TS11` und `SI12`). Ein Eintrag in die Preload-Liste sagt jedem Browser: **diese Domain und alle ihre Subdomains nur noch über HTTPS, ohne Ausweg.** Solange die kurze Adresse per HTTPS keine Verbindung aufbaut, wäre sie damit nicht mehr nur ein Totlink, sondern dauerhaft unerreichbar — und aus dieser Liste kommt man, wie der Kommentar im Code festhält, nur schwer wieder heraus. Der Punkt ist also nicht am Rechner lösbar, sondern fällt in dem Moment, in dem der CNAME beim Registrar steht; die Umgebungsvariable ist dafür bereits vorgesehen und muss nur auf `True` gezogen werden. | 2026-09-12 |
<!-- bewertung:ende -->

## Eigene Punkte

<!-- eigenepunkte:anfang -->
| Punkt | Titel | Bereich | Zustand | Beleg | seit |
|---|---|---|---|---|---|
| EIG01 | Datenschutzerklärung sagt „keine Reichweitenmessung", während jeder Seitenaufruf gespeichert wird | technik | erledigt | an RTC-Service gefunden am 07.09.2026: templates/datenschutz.html:122` „Es findet keine Reichweitenmessung statt"; `apps/core/middleware.py:187` legt für jeden GET-200 einen `PageVisit` mit Pfad, IP und User-Agent an, 90 Tage (`_PAGEVISIT_MAX_DAYS`). § 4 der Erklärung (Zeile 96–105) nennt nur das Kontaktformular mit fünf Feldern — der Rechner (`PreisAngebot`: Name, Telefon, Ort, Preis, `models.py:34`) kommt darin nicht vor. **An WVM-IT am 07.09.2026 geprüft: trifft hier nicht zu.** Die Messung läuft über `landing/messung.py` und speichert ausschliesslich Summen je Tag und Seitenpfad — keine IP, kein Cookie, keine Kennung. Die Datenschutzerklärung hat dafür einen eigenen Abschnitt 5 (Reichweitenmessung ohne Personenbezug), und sie benennt darüber hinaus **alle sechs** Verarbeitungen einzeln: Server-Logs bei Railway, die IP der Spam-Bremse (15 Minuten), die Anfragensicherung ohne IP, Zeitpunkt und IP der Werbe-Einwilligung (Art. 7 DSGVO, § 174 TKG 2021), Spline nach Einwilligung, und die Reichweitenmessung selbst. Einzige Ungenauigkeit: Der Absatz zur Anfragensicherung sagt *keine IP-Adresse*, während die gesicherte Zeile ein Feld `werbung_ip` trägt — das ist an anderer Stelle korrekt erklärt, die Formulierung dort aber missverständlich. | 2026-09-07 |
| EIG02 | Impressum nennt weder Kammer noch Berufsbezeichnung | recht | offen | an RTC-Service gefunden am 07.09.2026: templates/impressum.html:47–69` führt Name, Anschrift, Kontakt, Steuernummer — keine Handwerkskammer, keine gesetzliche Berufsbezeichnung, keine berufsrechtliche Regelung. Die Seite verkauft Elektroinstallation (`/leistungen/elektrotechnik/`) und Kälteanlagenbau (`llms.txt`, „Kälte- und Kühltechnik"), beides Anlage-A-Handwerke. **An WVM-IT am 07.09.2026 geprüft: trifft zu, siehe Offen Nr. 6.** Österreich statt Deutschland, also § 5 Abs. 1 ECG statt DDG: Z 6 verlangt die Kammer, der der Diensteanbieter angehört, Z 7 die Berufsbezeichnung samt verleihendem Staat. Das Impressum nennt Gewerbebehörde (BH Vöcklabruck) und Gewerbeordnung mit RIS-Link, aber weder die Wirtschaftskammer noch die Berufsbezeichnung. | 2026-09-07 |
| EIG03 | Impressum zitiert drei aufgehobene Rechtsgrundlagen | recht | offen | an RTC-Service gefunden am 07.09.2026: templates/impressum.html:48` „Angaben gemäß § 5 TMG" (TMG seit 14.05.2024 durch das DDG ersetzt), `:72` „§ 55 Abs. 2 RStV" (RStV seit 07.11.2020 durch den MStV, § 18 Abs. 2), `:83` Link auf die EU-OS-Plattform, deren Betrieb am 20.07.2025 eingestellt wurde. **An WVM-IT am 07.09.2026 geprüft: teilweise, siehe Offen Nr. 7.** Die TMG- und RStV-Hälfte trifft **nicht** zu — das Impressum zitiert korrekt österreichisches Recht (§ 5 ECG, § 25 MedienG). Der dritte Punkt trifft zu: Der Verweis auf `ec.europa.eu/consumers/odr` steht noch, obwohl die EU die Plattform zum 20.07.2025 eingestellt hat. | 2026-09-07 |
| EIG04 | PageVisit speichert die Proxy-Adresse statt der Besucher-IP | technik | erledigt | an RTC-Service gefunden am 07.09.2026: apps/core/middleware.py:189` `_anonymize_ip(request.META.get('REMOTE_ADDR'))` — dafür gibt es `spam.get_client_ip()` (`apps/core/spam.py:69`), das genau diesen Fall behandelt und in beiden Formularpfaden benutzt wird. Hinter dem Railway-Proxy steht in jeder Zeile derselbe Wert. **An WVM-IT am 07.09.2026 geprüft: trifft hier nicht zu.** Die Messung speichert überhaupt keine IP. Wo eine gebraucht wird (Spam-Bremse, Einwilligungsnachweis), liefert sie `views._client_ip()` — und zwar bewusst die **letzte** Adresse aus `X-Forwarded-For` statt der ersten, mit ausgeschriebener Begründung: Alles vor der letzten stammt aus einem Header, den der Absender selbst setzen kann. Wer die erste nimmt, lässt jeden Absender seine eigene Kennung wählen — die Spam-Bremse zählt dann je Fantasie-Adresse und greift nie. Vor der App steht genau ein Proxy; der Kommentar hält fest, dass bei einer zweiten Schicht weiter vorne gegriffen werden muss. | 2026-09-07 |
| EIG09 | Eine Pflicht-Checkbox koppelt Leistung, Rabatt und Werbe-Newsletter | inhalte | offen | Tiefenanalyse 07.09.2026 (Schwere: kritisch): landing/i18n/de.py:111` + `templates/index.html:200` (`required`): „Ich möchte die kostenlose Beispiel-Website, meinen 25%-Code und den wöchentlichen Referenz-Newsletter … erhalten und stimme der Datenschutzerklärung zu" — am 07.09. auf `https://www.wvm-it.tech/` so ausgeliefert. `CLAUDE.md` verlangt: „Einwilligungen: Nie an eine Leistung koppeln. Getrennt, nicht vorausgewählt" | 2026-09-07 |
| EIG10 | Der Wochen-Newsletter geht auch dann raus, wenn es nichts zu zeigen gibt | technik | offen | Tiefenanalyse 07.09.2026 (Schwere: wichtig): landing/views.py:1585–1587` bricht nur bei **null Abonnenten** ab, nie bei leerem `refs`; `views.py:1555` setzt dann „Bald stellen wir hier neue Arbeiten vor.", der Textteil (`:1597`) wird zu „Unsere neuesten Arbeiten:" plus Leerzeile. Betreff bleibt „unsere aktuellen Projekte". `landing/scheduler.py:35` feuert Mo 09:00 | 2026-09-07 |
| EIG11 | Die Danke-Seite wird mit eingeschaltetem JavaScript nie erreicht | aufgaben | offen | Tiefenanalyse 07.09.2026 (Schwere: wichtig): landing/views.py:4058–4062`: „Mit JavaScript fängt anfrage-blocks.js den Versand ab … dieser Zweig wird dann gar nicht erreicht." `/anfrage/danke/` ist der einzige Redirect und steht nur im No-JS-Pfad von `leistung_anfrage`; alle übrigen Formulare melden inline. `doku/60-ADS.md:6` und `doku/30-INHALTE.md:80` führen sie als zählbaren Abschluss | 2026-09-07 |
| EIG12 | Das Schema nennt „ab 350 EUR", die Seite bewirbt ab 29 € | seo | offen | Tiefenanalyse 07.09.2026 (Schwere: wichtig): landing/views.py:1745` `"priceRange": f"ab {c.get('preis_ab','350')} EUR"` aus `content.json:26`; dieselbe Seite trägt in `content.json:28` „IT-Betreuung ab 29 €/Monat je Arbeitsplatz, Support ab 95 €/Std.". 350 ist der One-Pager-Preis, nicht der Einstieg. Zweite Preisquelle neben `ANGEBOT_GROUPS`; `pruefe_seite._pruefe_preise` (`:233`) matcht nur `€`/`&euro;`, nie „EUR" | 2026-09-07 |
| EIG13 | Zutritt und Videoüberwachung werden verkauft, haben aber keine Seite | inhalte | offen | Tiefenanalyse 07.09.2026 (Schwere: wichtig): landing/views.py:219` verkauft „Netzwerk & Sicherheit — Stabiles Netzwerk, Zutritt und Videoüberwachung" (`anfrage: True`); in `landing/i18n/seiten_*.py` kommt „Videoüberwachung"/„Zutritt" nur auf `smarthome-knx-loxone` vor (`seiten_de.py:439,446`). Search Console: „gebäudesicherungssystem gmunden" 4 Impressionen, Position 9,8 — ohne Zielseite. `CLAUDE.md:59` behauptet, seit 05.09. habe **jedes** Geschäftsfeld eine eigene Seite | 2026-09-07 |
| EIG14 | Die Kontaktseite widerspricht sich im selben Satz | inhalte | offen | Tiefenanalyse 07.09.2026 (Schwere: wichtig): landing/i18n/de.py:967`, live auf `/kontakt/`: „Anfragen außerhalb dieser Zeiten beantworten wir am nächsten Werktag, **in jedem Fall innerhalb von 24 Stunden**." Freitag 19:00 → nächster Werktag ist Montag, also über 60 Stunden. Die AGB (`content.json:50` §5) sagen korrekt „an Werktagen innerhalb von 24 Stunden" | 2026-09-07 |
| EIG15 | Die Datenschutzerklärung trägt „Stand: Juli 2026", beschreibt aber September-Verarbeitungen | recht | offen | Tiefenanalyse 07.09.2026 (Schwere: wichtig): content.json:32`, Schlusszeile „Stand: Juli 2026" — live am 07.09. so ausgeliefert. Abschnitt 5 (Reichweitenmessung) und der Absatz zur Anfragensicherung sind am 05./06.09.2026 entstanden. AGB und Barrierefreiheitserklärung tragen im selben `content.json` „Stand: September 2026" | 2026-09-07 |
| EIG16 | Datenschutz nennt den Selbsttest als einzige Werbe-Einwilligung — es gibt eine zweite | recht | offen | Tiefenanalyse 07.09.2026 (Schwere: wichtig): content.json:32` §3: „An einzelnen Stellen — **derzeit beim IT-Sicherheits-Selbsttest** — können Sie zusätzlich und freiwillig einwilligen". Der wöchentliche Referenz-Newsletter der Startseite (`de.py:111`, `views.py:_send_weekly`) ist eine zweite, stärkere Werbe-Einwilligung; §3 führt „Newsletter" nur als Anfrageformular, dessen Daten „ausschließlich zur Bearbeitung Ihrer Anfrage" dienten | 2026-09-07 |
| EIG17 | Die Messung verliert bei jedem Deploy den laufenden Tag und meldet ihn trotzdem als Tageswert | technik | offen | Tiefenanalyse 07.09.2026 (Schwere: wichtig): landing/messung.py:111–146` schreibt nur alle 25 Ereignisse, beim Tageswechsel oder auf Befehl — kein `atexit`/SIGTERM-Flush. `landing/management/commands/messung.py:71–80` nimmt je Tag die **letzte** Zeile („Momentaufnahmen desselben laufenden Tages, keine Zuwächse"); nach einem Deploy beginnt die Reihe bei null, der Verkehr davor fällt still weg. Deploy erfolgt bei jedem Push auf `main | 2026-09-07 |
| EIG18 | Der SearchAction im Schema zeigt auf einen Pfad, den die eigene robots.txt sperrt | seo | offen | Tiefenanalyse 07.09.2026 (Schwere: hinweis): landing/views.py:1802–1806` setzt `urlTemplate: {base}/suche/?q={search_term_string}`; `landing/views.py:3311` listet `/suche/` in `_ROBOTS_DISALLOW`. Die angekündigte Aktion führt auf eine Adresse, die kein Crawler abrufen darf | 2026-09-07 |
| EIG19 | Die vier Vorgangsseiten duzen, der Rest der Seite siezt | inhalte | offen | Tiefenanalyse 07.09.2026 (Schwere: hinweis): landing/i18n/de.py:1112` „Dein 25%-Rabattcode", `:1224` „Du bekommst den Link auch per E-Mail", `:1227` „Den Link findest du auch in deiner E-Mail", `landing/views.py:1568` „Du bekommst diese Mail, weil du den WVM-IT-Newsletter bestätigt hast". `doku/80-AUFGABEN.md:177` führt „Anrede vereinheitlicht" als am 28.08.2026 erledigt | 2026-09-07 |
| EIG20 | Der JavaScript-Notnagel des Roboters trägt die alte Positionierung und einen getippten Preis | technik | offen | Tiefenanalyse 07.09.2026 (Schwere: hinweis): static/js/main.js:89–95`: Fallback für `window.I18N.robot` mit „Wir bauen Webseiten, Hosting, KI und SEO", „Schon ab 350 Euro hast du deine eigene Webseite." Greift, sobald `t.hero.robot_msgs` leer ist (`templates/base.html:83`) — dann steht auf allen drei Sprachfassungen deutscher Text mit einem Preis außerhalb von `ANGEBOT_GROUPS`, den `pruefe_seite` im JavaScript nicht sieht | 2026-09-07 |
| EIG21 | Die Doku verbietet Öffnungszeiten, die die Seite längst nennt | local-seo | offen | Tiefenanalyse 07.09.2026 (Schwere: hinweis): doku/50-LOCAL-SEO.md:36`: „Öffnungszeiten | 2026-09-07 |
| EIG22 | Für „5 Arbeitsplätze" zeigen Startseite und Rechner zwei Zahlen | inhalte | offen | Tiefenanalyse 07.09.2026 (Schwere: hinweis): landing/views.py:505–509` Stufe „klein" = 5 AP, kein Server → 194 €/Monat (live auf `/`); `landing/views.py:570–572` Rechner-Voreinstellung = 5 AP, **1 Server**, Sicherung → 283 €/Monat auf `/kosten/rechner/`. Der Kommentar `views.py:503–504` behauptet das Gegenteil: „Die Größen sind bewusst dieselben wie die Beispiele im Kostenrechner, damit ein Besucher … nicht zwei Wahrheiten findet" | 2026-09-07 |
| EIG23 | Eine gelöschte Datenvariable steht noch in llms.txt und llms-full.txt | technik | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG24 | Die Danke-Seite verspricht eine Bestätigungsmail, die bei Spamverdacht nie kommt | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG25 | Startseite und Über-mich melden lastmod 04.09., obwohl sie am 06./07.09. geändert wurden | seo | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG26 | Kein Angebot zu BFSG, obwohl die Suche danach fragt | seo | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG27 | Die Inhalte-Doku kennt vier Ratgebertexte nicht und widerspricht ihrem eigenen Kopf | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG28 | Ratgebertexte melden og:type „website", das Schema nennt sie Article | seo | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG29 | Die Datenschutzerklärung sagt „jede Anfrage wird in jedem Fall gespeichert" — eine wird verworfen | recht | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG30 | „10+ Jahre Branchenerfahrung" gegen `foundingDate` 2024 im Schema derselben Seite | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG31 | Die Löschung der Sitzungsdaten hängt hinter dem Mailversand — dieselbe Kopplung, die bei `PageVisit` schon einmal die Frist gerissen hat | technik | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG32 | Das Servicegebiet steht zweimal — einmal als Quelle, einmal von Hand | seo | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG33 | llms.txt teilt das Servicegebiet nach fester Listenposition auf | technik | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG34 | Das JSON-LD-Logo nennt Maße, die die Bilddatei nicht hat | seo | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG35 | Die Bewertung des eigenen Entwicklers steht sichtbar als Kundenstimme | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG36 | Die Aufgabe „tote Dateien entfernen" würde vier lebende Bausteine löschen | aufgaben | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG37 | Die Alias-Liste der Weiterleitung ist ein Literal und hängt nicht an `ALLOWED_HOSTS | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG38 | „8+ Leistungsbereiche" steht auf /ueber-uns/ weiter, nachdem es von der Startseite als unbelegt entfernt wurde | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG39 | Der Rechner steht auf acht Seiten, sein eigener Kopf sagt drei, die Doku fünf | notizen | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG40 | Das Schema baut seine Bild-URLs von Hand am Static-Manifest vorbei | seo | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG41 | Hochgeladene Profilbilder verschwinden bei jedem Deploy, ohne Fehler | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG42 | Der als kritisch gemeldete `mark_safe` ist nachweislich unbedenklich | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG43 | Das Kontaktformular meldet Erfolg, auch wenn die Mail nie hinausgeht | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG44 | Die Doku beschreibt einen Live-Stand, den es seit dem Deploy nicht mehr gibt | status | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG45 | Google-Maps-iframe lädt vor jeder Einwilligung — die Doku sagt, es werde nichts Einwilligungspflichtiges geladen | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG46 | Newsletter ohne Bestätigungsschritt, ohne Adressprüfung, ohne Abmeldeweg | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG47 | Bestätigungs- und Newsletter-Links hängen an `SITE_URL`; der Vorgabewert zeigt auf einen toten Host | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG48 | Die 301 auf `www` liegt live im Code — es fehlt allein die Umgebungsvariable | seo | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG49 | Die Sitemap bricht mit `KeyError`, sobald eine Seite ohne Registereintrag ergänzt wird | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG50 | security.txt` erneuert sein `Expires` bei jedem Abruf | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG51 | zaehle()` erneuert die Ablaufzeit bei jedem Treffer — kein Zeitfenster ist eins | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG52 | /ueber-mich/` steht in keiner Navigation und in keiner Fußzeile | konversion | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG53 | Der Kopf von `00-STATUS.md` nennt 84,4, der Messblock darunter 93,1 | status | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG54 | Fünf Punkte stehen als „offen" oder „fehlt", sind live erledigt | aufgaben | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG55 | Die Tabelle „Zahlen, die in mehreren Quellen unterschiedlich stehen" nennt selbst drei überholte Werte | notizen | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG56 | theme-color` ist zwei Stufen heller als der Seitengrund | design | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG57 | /favicon.ico` und `/apple-touch-icon.png` lösen statische Pfade in Python auf, die keine Prüfung abdeckt | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG58 | alle_paare()` verspricht „jede Frage genau einmal" und lässt eine von siebzehn aus | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG59 | Der Messbefund PJ10 „zwei Views ohne Route" trifft nicht zu | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG60 | anfrage_verarbeiten` sagt im Vertrag „False bei Fehler" und gibt immer True | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG61 | Die Desktop-Navigation führt nicht zur Kontaktseite | design | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG62 | Die vier Anfragesperren liegen im Prozessspeicher, und gunicorn läuft mit zwei Workern | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG63 | Weder Datenschutzerklärung noch Code kennen eine Löschfrist für Anfragen | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG64 | Fünf verschiedene Leistungslisten, keine aus einer gemeinsamen Quelle | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG65 | Die Öffnungszeiten stehen an sechs Stellen von Hand, die Leistungsseiten nennen nur fünf davon | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG66 | Der Startbefehl steht dreimal, zwei Fassungen ohne `migrate` und `collectstatic | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG67 | Zwei Öffnungszeiten für denselben Betrieb: Schema schließt um 20 Uhr, der Text ist „durchgehend erreichbar" | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG68 | Blättern auf `/aktuelles/` und `/galerie/` nimmt alles ab Seite 2 aus dem Index | seo | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG69 | CLAUDE.md nennt durchgehend eine überholte Testzahl | wegweiser | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG70 | Die drei Pakete vom 08.09. liegen nur lokal — `main` kennt sie nicht, die Doku führt sie als erledigt | aufgaben | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG71 | Jede Mail des Shops geht von einer Domain aus, die es nicht gibt | technik | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG72 | Sitemap und `dateModified` melden für neun Seiten den 01.09., geändert wurden sie am 08.09. | seo | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026: shop1/seiten_stand.py` führt `home`, `produkte`, `kontakt`, `gaestebuch`, `ueber_uns`, `liefergebiet`, `datenschutz`, `agb`, `wissen` auf `'2026-09-01'`; auf `main` datieren `kontakt.html` und `produkte.html` auf `b35f6e4` (08.09.), `agb.html` auf `2b26108` (08.09.). Live-Sitemap vom 10.09.2026 nennt für alle neun `<lastmod>2026-09-01`. TS16 gilt als bestanden — die Angabe ist trotzdem falsch. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG73 | Das Impressum stützt sich auf zwei Rechtsgrundlagen, die es nicht mehr gibt | recht | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG74 | Das Besuchsprotokoll speichert volle IP-Adressen ohne Frist und ohne Löschroutine | technik | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG75 | Die E-Mail-Bestätigung ist folgenlos, ihre Erfolgsmeldung ungeprüft | technik | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG76 | Das Schema meldet Sonntags geschlossen, während die Seite „durchgehend erreichbar“ zusagt | technik | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG77 | base.html setzt für interne Seiten „index, follow“ als Voreinstellung | technik | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG78 | security.txt rechnet sein Ablaufdatum bei jedem Abruf neu und kann deshalb nie ablaufen | technik | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| ~~EIG79~~ | ~~Das Impressum stützt sich auf § 5 TMG und § 55 Abs. 2 RStV — beide Gesetze gibt es nicht mehr~~ | inhalte | **trifft nicht zu** | bei einer anderen betreuten Seite aufgefallen am 10.09.2026, **hier am selben Abend nachgeprüft**: `/impressum/` nennt **§ 5 ECG** und **§ 25 Mediengesetz** — die österreichischen Grundlagen, und die gibt es. TMG und RStV kommen im Text nicht vor. Der übertragene Punkt stammt von einer **deutschen** Seite; WVM-IT sitzt in Lenzing. Ein Fund, der auf einer Seite zutrifft, trifft auf einer anderen Rechtsordnung nicht zu — nachgesehen statt übernommen | 2026-09-10 |
| EIG80 | Jede Kurzanfrage wird als bestätigter Newsletter-Abonnent gespeichert, auch ohne Einwilligung | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): landing/views.py:4341–4346` (`leistung_anfrage`) ruft `supa.upsert_subscriber(kontakt, …, consent_ip=…)` ohne Prüfung des Felds `werbung` auf; `angebot_anfordern` prüft dagegen `if consent:` (`views.py:3499`). `supa.py:38–43` setzt `status='confirmed'` und überschreibt beim Konflikt auch `unsubscribed` (gesetzt in `views.py:1565`). Das widerspricht `content.json:32` („ausschließlich zur Bearbeitung Ihrer Anfrage“) und dem Formularhinweis `de.py:1043` („keine Werbung ohne Ihre Zustimmung“) | 2026-09-11 |
| EIG81 | Ob die Hilfe bei Störungen in den 29 € je Arbeitsplatz steckt, sagt die Seite zweimal verschieden | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): Enthalten: `views.py:159` („Updates, Überwachung, Hilfe bei Störungen — pro PC und Monat“), `vergleiche_de.py:33` („Störungen sind abgedeckt, es gibt keine Kostendiskussion vorab“), `vergleiche_de.py:53` (Monatssumme geteilt durch 95 € ergibt die Schwelle). Extra: `de.py:254` („Hilfe per Fernwartung nach Aufwand“), `de.py:271` („Einsatzstunden … kommen nach Aufwand dazu“), Startpaket „Laufende IT-Betreuung“ enthält zusätzlich `it_support` 95 €/Std. (`views.py:240`). Beides live am 11.09. auf `/` und `/vergleich/it-betreuung-vs-stundenabrechnung/ | 2026-09-11 |
| EIG82 | Die Vergleichsseite nennt die Datensicherung „bereits enthalten“, der Katalog berechnet 49 € extra | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): vergleiche_de.py:24`: „darin sind Überwachung, Updates und geprüfte Datensicherung bereits enthalten“, `vergleiche_de.py:31` ebenso. Dagegen `views.py:161` (`backup` 49 €/Mt) und `views.py:535` (die Betreuungsstufen addieren die Sicherung eigens); derselbe Absatz rechnet in `vergleiche_de.py:53` die 49 € selbst dazu | 2026-09-11 |
| EIG83 | /einrichten/netzwerk/` bricht die Festpreis-Zusage seines eigenen Silos | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): Live-Titel „Netzwerk einrichten im Büro — ab 890 €“ (`einrichten_de.py:461/463/466`, dazu „Startpreis“ `:470`); „Was für 890 € enthalten ist“ umfasst Messung und Einrichtung vor Ort (`:477–504`). Der Wegweiser sagt: Festpreis ohne „ab“, deckt nur Fernwartung, vor Ort 120 €/Std. extra (`einrichten_de.py:734–743`). `FestpreisTest` prüft nur `ein-preis`-Spans (`test_einrichtungen.py:243`), nicht Titel und Antwortabsatz | 2026-09-11 |
| EIG84 | Microsoft 365 für bis zu 15 Postfächer kostet weniger als zwei eingerichtete Arbeitsplätze | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): einrichten_de.py:314` („Einrichtung auf den Arbeitsplätzen und auf den Telefonen“) und `:349–351` („bis etwa fünfzehn Postfächer“) für 290 €; `/einrichten/arbeitsplatz/` verlangt 190 € je Gerät und zählt das Postfach dazu (`:40`, `:80`). Katalogname „Microsoft 365 einrichten & betreuen“ (`views.py:163`) nennt Betreuung, hat aber nur `once | 2026-09-11 |
| EIG85 | llms.txt` führt eine abgetippte Preisliste, und keine Prüfung sieht sie | seo | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): views.py:3756–3762`: Preise als Festtext, z. B. „Arbeitsplatz einrichten ab 190 €“ (mit „ab“, gegen die Festpreis-Regel). `views.py:3730` sagt „elf Leistungsseiten“ (es gibt 13), `:3739` nennt drei statt vier Vergleiche, das Silo `/einrichten/` fehlt ganz. `pruefe_seite.py` enthält kein „llms“ (0 Treffer). CLAUDE.md nennt `ANGEBOT_GROUPS` als einzige Quelle „auch für … llms.txt“ | 2026-09-11 |
| EIG86 | Das Einzugsgebiet vor Ort steht in vier Fassungen, Schema und `llms.txt` widersprechen sich | local-seo | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): Schema `views.py:1710–1713`: Linz nicht vor Ort, nur Fernwartung (`_AREA_CITIES`). `llms.txt` `views.py:3612`: Linz vor Ort, Bad Ischl fehlt (live bestätigt). `de.py:1033` nennt Bad Ischl und Linz, `de.py:509` „von Salzburg bis Linz“, `regionen_de.py:203` bietet in Linz Arbeiten vor Ort an | 2026-09-11 |
| EIG87 | Fest geschriebene Anzahlen sind veraltet, auch im Titel und im Antwortabsatz (`speakable`) | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): templates/aktuelles.html:3,19`: „15 Fachbeiträge“, `landing/beitraege.py` hat 18. Live `/leistungen/`: „Elf Leistungen, drei Bereiche“ (`de.py:853`) bei 13 Links. Live `/vergleich/`: „Alle drei Vergleiche“ und „die drei Entscheidungen“ (`de.py:571`) bei 4 Seiten | 2026-09-11 |
| EIG88 | Der Konfigurator `/angebot/` hat noch Kopf und Fuß von vor dem Relaunch | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): templates/angebot.html` erbt nicht von `base.html`. Fuß `:309–311`: vier Leistungslinks alle auf `/#leistungen` (Smarthome zuerst), kein AGB-, Barrierefreiheits- oder Über-uns-Link, keine Anschrift (`base.html:179–232` hat alles). Navigation `:63–67` führt auch auf `/en/angebot/` zu `/` (live geprüft) | 2026-09-11 |
| EIG89 | /referenzen/` verspricht Zustimmung, die AGB erlauben die Nennung ohne | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): Live `/referenzen/`: „nennen niemanden ohne Zustimmung“, ebenso `views.py:3785`. AGB §14 in `content.json:50`: „WVM-IT darf den Auftraggeber mit Namen, Logo … als Referenz nennen. Der Auftraggeber kann dem jederzeit … widersprechen“, also Widerspruch statt Einwilligung | 2026-09-11 |
| EIG90 | /kontakt/` sagt „in jedem Fall innerhalb von 24 Stunden“ und zugleich „am nächsten Werktag“ | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): de.py:971`: „beantworten wir am nächsten Werktag, in jedem Fall innerhalb von 24 Stunden“. Eine Anfrage vom Freitagabend hält nicht beides ein. AGB §5 (`content.json:50`) und `de.py:317` sagen nur „an Werktagen“ | 2026-09-11 |
| EIG91 | Der Schlüssel `ueber` steht in allen drei Sprachpaketen doppelt, der erste Block ist tot | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): de.py:296` und `:1011`, `en.py:309/980`, `ro.py:305/976` im selben `PACK`-Dict: Der zweite gewinnt. `index.html:333` liest `t.ueber.badge_fallback`, das nur im überschriebenen Block steht. Heute unsichtbar, weil `content.json:10` die `tagline` füllt; ist sie leer, bleibt die Bildunterschrift leer | 2026-09-11 |
| EIG92 | Kilometer und Fahrzeiten stehen doppelt, einmal davon als freier Text | local-seo | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): regionen.py:47` (`km: 6, fahrzeit: 10`) füttert `llms.txt`; `regionen_de.py:29,44` tippen „6 Straßenkilometer … 10 Minuten“ ab, `:31` sagt für dieselbe Strecke „eine Viertelstunde Weg“. `pruefe_seite` vergleicht nur Zahlen vor „€“ | 2026-09-11 |
| EIG93 | Videoüberwachung und Zutritt werden angeboten, haben aber keine Seite, obwohl danach gesucht wird | seo | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): views.py:220` („Zutritt und Videoüberwachung“), `de.py:198`, `de.py:1442`; einzige Fachstelle ist ein Stichpunkt in `seiten_de.py:446`. Search Console bis 07.09.: „gebäudesicherungssystem gmunden“ auf Position 9,8 (`doku/40-SEO.md:72`) | 2026-09-11 |
| EIG94 | Die Seite spricht Privathaushalte an, informiert aber nirgends über das Rücktrittsrecht | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): regionen_de.py:58` („Betriebe und Privathaushalte“), `content.json:42` („Unternehmen und Privatkunden“), AGB §15 nennt Verbraucher (`content.json:50`). „Rücktritt“, „Widerruf“ und „FAGG“ kommen in `content.json` null Mal vor | 2026-09-11 |
| EIG95 | Die Vertretungsregelung mit einem Partnerbetrieb ist eine Zusage ohne Beleg | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): de.py:681`, gerendert auf `/it-notfall/` (`notfall.html:94`): „feste Vertretungsregelung mit einem Partnerbetrieb … Namen und Nummer bei Vertragsbeginn schriftlich“. Keine Quelle in `docs/`, kein Eintrag unter „Beim Kunden“; `docs/HERO-KONZEPT-2026-09-06.md:48` stützt die Hero-Überschrift darauf | 2026-09-11 |
| EIG96 | Die Startseite zeigt drei Arbeiten, `/referenzen/` sagt, es gebe nur eine | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): de.py:230–237` („Ein Eindruck unserer Arbeit … Geplant, installiert und betreut“, drei Projektbilder, `index.html:829–851`). Live `/referenzen/`: „ausschließlich Projekte, die wirklich existieren … Aktuell ist das Rümpelwerk“. Herkunft der Bilder seit 28.08. offen (`doku/20-DESIGN.md:100`) | 2026-09-11 |
| EIG97 | Der Steckbrief in 00-STATUS nennt einen überholten Stand | status | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): doku/00-STATUS.md:25`: „166 URLs, 82 Basis-Pfade“ (CLAUDE.md: 198/94). `:30`: letzter Commit `172cf0c`, Pakete 147/153 „auf Zweigen“. `.git/logs/HEAD:332` zeigt den Merge von `sofort/2026-09-10-pf18-und-2-weitere`, und der GE26-Satz zur Fernwartung ist live auf `/kontakt/ | 2026-09-11 |
| EIG98 | 30-INHALTE führt längst gebaute Seiten als fehlend | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): doku/30-INHALTE.md:18–30`: 158 URLs, kein Silo `/einrichten/`, 11 Leistungen, 3 Vergleiche. `:77–84` „Fehlende Inhalte“: Über uns, AGB, Barrierefreiheit, Danke-Seite, Feed. Laut `doku/80-AUFGABEN.md:181` seit 05.09.2026 live | 2026-09-11 |
| EIG99 | 80-AUFGABEN führt Erledigtes weiter als offen | aufgaben | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): „Offen“ Nr. 12 (`doku/80-AUFGABEN.md:77`, „Was bei einem Hardwaredefekt passiert, steht nirgends“) ist beantwortet in `einrichten_de.py:91–96` („Werkstattreparaturen führen wir nicht durch“). Hebel 3 (`:99`, TS23) und Hebel 11 (`:107`, „keine Danke-Seite“) sind laut `:181–182` seit 05.09. behoben | 2026-09-11 |
| EIG100 | Im Fuß jeder Seite steht „Shop“ und führt zu einem fremden Betrieb | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): templates/base.html:214` und `angebot.html:310`: Spalte „Unternehmen“, Linktext `u_shop` = „Shop“ (`de.py:381`), Ziel `content.json:31` `https://www.pystore.de`. Laut `views.py:357` ist das der Partner für Webentwicklung, kein Shop von WVM-IT | 2026-09-11 |
| EIG101 | Das Impressum stützt sich auf § 5 TMG und § 55 Abs. 2 RStV — beide Gesetze gibt es nicht mehr | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 11.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-11 |
| EIG102 | Die Doku führt GE32 und MW15 als „nicht auf main, nicht ausgeliefert“, beides ist gemergt und live | status | offen | bei einer anderen betreuten Seite aufgefallen am 11.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-11 |
| EIG103 | Der CI-Lauf behauptet, nichts auszulassen, fährt die Testsuite aber nicht und nutzt eine andere Python-Fassung als die lokalen Läufe | technik | offen | bei einer anderen betreuten Seite aufgefallen am 11.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-11 |
| EIG104 | Der KV07-Zweig steht in der Doku als „nicht gemergt“ und ist seit dem Merge live | status | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG105 | Jeder anonyme Besucher bekommt ein 14-Tage-Sitzungs-Cookie allein für die Reichweitenmessung | technik | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG106 | „Jetzt bei Google bewerten“ führt auf dieselbe Maps-Suche wie der Knopf darunter | local-seo | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG107 | Das Kontaktformular verliert alles Getippte, sobald die Serverprüfung greift | technik | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG108 | Der Ebenensprung h1→h3 auf zehn Seiten hat eine einzige Ursache: es gibt gar keine `h2 | seo | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG109 | Die volle Besucher-IP geht unverschlüsselt über HTTP an einen Drittdienst | technik | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG110 | Ausgeschriebene Zeitzusagen sind für `check_seo` unsichtbar | technik | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG111 | „Lädt in unter zwei Sekunden, gemessen statt behauptet" nennt keine Messung | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG112 | Impressum und Datenschutz stützen sich auf vier aufgehobene Gesetze | recht | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG113 | Die Doku führt als „nicht ausgeliefert", was live steht | status | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG114 | Der `speakable`-Selektor markiert auf elf Seiten jeden Absatz im Inhalt | seo | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG115 | Die Fehlerseiten führen den Besucher ins Mitarbeiter-Login statt zurück in den Betrieb | technik | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG116 | Auf jeder Fehler- und Anmeldeseite steht ein fremdes Logo-Zeichen „F" | technik | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG117 | Die 500-Seite verspricht eine Benachrichtigung, die niemand bekommt | technik | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG118 | GE21 „areaServed nennt 1 Gebiet" ist ein Ablesefehler, kein Mangel am Schema | seo | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG119 | Der gesamte Stand vom 12.09.2026 ist live, die Doku führt ihn dreifach als „nicht ausgeliefert" | status | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG120 | Der Winterdienst verspricht Erreichbarkeit rund um die Uhr, das Schema schließt um 17 Uhr | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG121 | Die als „Antwort zuerst" geschriebene Einleitung steht erst an zweiter Stelle | seo | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG122 | Die Bestätigungsseite behauptet, die Nachricht liege im Postfach — auch wenn der Versand scheitert | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG123 | Datenschutzerklärung bestreitet eine Messung, die der Code durchführt | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
<!-- eigenepunkte:ende -->
